# `3.23.0` → `3.24.0`

The **Slack-bridge-hardening sprint** (`2026-05-17-slack-bridge-hardening`, 11 stories 087–097) — Slack-bridge bug fixes, fail-closed startup guards, cross-project safety, the channel-scope/workspace-id confirm-and-remember mechanisms, and the Slack-app setup doc. Delivered as one integration PR.

**087 — retro-flow MCP types.** MODIFIED `plugin/mcp/claude-wow-server/server.py` — `ALLOWED_TYPES` gains `sprint-ack`, `retro-opening`, `retro-close`, so peers emit these directly instead of the `status`+`intent:` workaround that cluttered audit/retro tooling. MODIFIED `plugin/commands/_agent-protocol.md` — message-type table gains a row per new type. MODIFIED `plugin/tests/mcp-server-bus-emit.sh` — a positive case per new type.

**088 — Slack-bridge setup doc.** NEW `plugin/bridge/slack/README.md` — documents the Slack-app configuration the bundled bridge requires: app-level token scope, the required + optional bot-token OAuth scopes, Event Subscriptions, and a traceability table of every Slack Web API method the bridge calls.

**089 — channel-scope confirm/remember.** MODIFIED `plugin/commands/slacker.md` + `_slacker-startup.md` — Slacker confirms its bridge channel scope once with M (→ human), persists it in a `<!-- slacker-channel-scope -->` learnings block, and spawns the bridge scoped (`BRIDGE_CHANNEL`) or unscoped accordingly; stale spawn env-var names corrected. No bridge code change.

**090 — channelByName mpim drop.** MODIFIED `plugin/bridge/slack/src/bridge/cache.ts` — `channelByName()`'s `conversations.list` drops `mpim` from its `types` filter (a multi-person DM has no `#name`, so it can never be a by-name target). NEW `node:test` smoke case.

**091 — health-cron removal.** REMOVED the 5-minute `/health`-poll `CronCreate`/`CronDelete` from `_slacker-startup.md` and `slacker.md`. MODIFIED `slacker.md` `# Bridge health monitoring` — health now rides the bridge's persistent spawn-`Monitor` (process death + `socket-mode` disconnect/fail stdout lines), escalating once per outage. MODIFIED `manager.md` — the bridge-unhealthy handler's payload keys and cadence updated to match (event-driven, no polling cron).

**092 — workspace-mismatch guard (bridge-side).** NEW `plugin/bridge/slack/src/bridge/workspace-guard.ts` — pure `assertWorkspace` (`team_id`-exact). MODIFIED `plugin/bridge/slack/src/index.ts` — when `BRIDGE_WORKSPACE_ID` is set the bridge fail-closed exits on a workspace mismatch immediately after `auth.test`, before handlers/HTTP/Socket-Mode start; no-ops cleanly when unset. MODIFIED `manager.md` + smoke tests.

**093 — bridge-PID per-project scoping.** NEW `slacker.md` `## Bridge ownership check` — before S signals/restarts/flags any bridge it confirms ownership (the project-local `.bridge-pid` is alive AND `/health` reports this project's `eventsPath`); no process-name (`pkill`) sweep. Cross-project safety so one project's Slacker can never touch another's bridge.

**094 — events-feed trim ts/cutoff fix.** NEW `plugin/scripts/slack-events-trim.sh` — the `events.jsonl` 1-week trim extracted from a doctrine helper into a unit-testable script; `cutoff` is now a Unix epoch with a numeric `jq` comparison (was a meaningless ISO-8601-vs-epoch string compare — backlog 133). MODIFIED `slacker.md` (inline function → script invocation), the `slack-events-trim.sh` test, and the doctrine-invoked helper-script count in root + `plugin/AGENTS.md` (now 12).

**095 — OAuth-scope startup preflight.** NEW `plugin/bridge/slack/src/bridge/required-scopes.ts` (`REQUIRED_SCOPES` + pure helpers). MODIFIED `index.ts` — after the 092 guard the bridge preflights the bot token's granted scopes (the SDK-surfaced `response_metadata.scopes`, derived from the `x-oauth-scopes` header) against `REQUIRED_SCOPES`; any missing scope ⇒ fail-closed exit with a stable `missing OAuth scope(s): <list>` line; an absent scope list skips the preflight (no false-positive brick). A flush-safe exit is retrofitted to both fail-closed guards (FINDING-22). MODIFIED `README.md` — adds `app_mentions:read`. NEW `plugin/tests/slack-required-scopes-sync.sh` — gates the README scope table against `REQUIRED_SCOPES`.

**096 — health-monitoring doctrine coherence (091 FINDING-19).** MODIFIED `slacker.md` `# Bridge health monitoring` — `bridge-status` is no longer mis-listed as a spawn-`Monitor` health trigger (it is a bus message S emits); removes a double-escalation and a `slacker.md`↔`manager.md` framing disagreement.

**097 — workspace-id populate/repair + unified escalation contract.** MODIFIED `slacker.md` — a `## Workspace learning` block, `## Bridge auto-launch` step `4d. Resolve workspace` (089-style one-time confirm), a generalized `## Spawn-fail behavior` reason-namer parsing both `workspace mismatch:` and `missing OAuth scope(s):` stdout shapes into a single cause-named `bridge-status`, and a `## Bridge-repair signals` section. MODIFIED `manager.md` — the `### bridge-status` handler gains a concrete workspace re-enter repair (targeting the exact originating S agent) and a missing-OAuth-scope escalation branch. MODIFIED `_slacker-startup.md`.

**Suite count: 79 → 80** (`plugin/tests/slack-required-scopes-sync.sh`, added by story 095).

**Consumer action after upgrade:** `claude plugin update claude-wow`, `/reload-plugins`, restart all peers (M, SD, PP, T, and S — multiple role files changed). On its next launch Slacker confirms its channel scope (089) and expected workspace id (097) once each, if no `<!-- slacker-channel-scope -->` / `<!-- slacker-workspace -->` learnings block exists yet. Just update `.version`.
