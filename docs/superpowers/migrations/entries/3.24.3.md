# `3.24.2` → `3.24.3`

## Modified

- **`plugin/scripts/wow-process/idle-monitor.py` — `emit_idle_event()` nudge prompt.** The `all-idle-nudge` `payload.prompt` text gains two additions:
  - **Current time line.** The prompt now includes a `Current time: <ISO-8601 Z>` line so M sees the wall-clock moment of detection inline, not only in the structured `ts` / `detected_at` fields. The single `now` timestamp is now computed once and reused for `ts`, `detected_at`, and the prompt (previously two separate `utcnow()` calls).
  - **Sub-agent caution + mandatory `declare_idle`.** A new paragraph instructs M not to ignore the nudge: agents can appear idle when no activity is recorded while they have actually spawned sub-agents doing work, so M should check in via `bus_emit`; and if M determines all agents are genuinely idle, M **must** call `declare_idle` so the check-in stops firing — otherwise it triggers every minute and wastes tokens.

No behavior change to the idle predicate, the loop cadence, or the event shape — only the human-readable `payload.prompt` string is overloaded with more guidance.

## Consumer action

`/reload-plugins` + peer restart per standard upgrade hygiene. The idle-monitor process picks up the new prompt on M's next session start.

## Closes

Direct human directive (Suraj, 2026-05-19) — overload the idle-monitor nudge prompt with the current time and an explicit sub-agent / mandatory-`declare_idle` caution. No story; shipped directly on `main`.
