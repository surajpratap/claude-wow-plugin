# `<NEXT-from>` → `<NEXT-to>`

Slack bridge fails loudly on a workspace mismatch at startup — bridge-side guard (Story 092, sprint 2026-05-17-slack-bridge-hardening). Stale Slack credentials (tokens for a different workspace) used to connect the bridge to the wrong workspace silently (backlog 130). **NEW** `plugin/bridge/slack/src/bridge/workspace-guard.ts` — a pure `assertWorkspace(expected, authResp)` (`team_id`-exact comparison) + `WorkspaceMismatchError`. **MODIFIED** `plugin/bridge/slack/src/index.ts` — when the `BRIDGE_WORKSPACE_ID` env var is set, the bridge compares it against the `auth.test` response's `team_id` immediately after `auth.test`; on mismatch it logs `[claude-slack-bridge] workspace mismatch: … — exiting` to stdout, best-effort removes its `.bridge-pid`, and `process.exit(1)` — **before** the handlers / HTTP server / Socket Mode start (fail-closed). When `BRIDGE_WORKSPACE_ID` is unset the guard no-ops cleanly — so the guard ships independently and arms automatically once story 097 populates the variable. **MODIFIED** `plugin/bridge/slack/tests/smoke.test.ts` — `node:test` cases over `assertWorkspace` (match / mismatch / name-is-not-a-match). **MODIFIED** `plugin/commands/manager.md` — the bridge-unhealthy handler recognizes a `workspace mismatch` reason and frames the `AskUserQuestion` as a wrong-workspace / creds problem (graceful-degrading to the generic escalation until story 097 makes S name the reason). No `slacker.md` change. Bundled bash test-suite count unchanged. **Consumer action after upgrade:** `claude plugin update claude-wow`, `/reload-plugins`, restart peers. Just update `.version`.
