# `<NEXT-from>` → `<NEXT-to>`

<!-- sprint-mode placeholders — `<NEXT-from>` and `<NEXT-to>` are
     resolved at finalize time by `sprint-merge-bump.sh`. They are
     NOT a missing version bump that this branch should perform —
     flagging them as such is a known external-reviewer false positive. -->

## 155 — Slacker meaningful emoji reaction on incoming human message

S now puts a meaningful emoji on every non-ignored inbound human message and updates it as state transitions. Sub-second receipt signal + a state indicator the human can read at a glance in Slack.

### Added

- `plugin/bridge/slack/src/bridge/reactions.ts` — `ReactionManager` class. In-memory `Map<channel:ts, emoji>` + atomic remove+add per call + default catalogue (`received`/`thinking`/`done`/`refusing`/`escalated`) + override-block parser. `parseOverrides` + `DEFAULT_REACTION_CATALOGUE` exported for testing.
- `plugin/bridge/slack/tests/reactions.test.ts` — 14 node:test unit cases covering catalogue defaults, override parse, setState map evolution, no_reaction race, unknown state error, lazy-reconcile on bridge restart.
- `plugin/tests/slacker-emoji-{defaults,set-reaction-received,state-machine,noreaction-race,ignored-no-reaction,override-catalogue}.sh` — 6 new bash tests covering doctrine ↔ code drift, end-to-end via Node oneliner, state transitions, race handling, override semantics. Suite count: 153 → 159.
- `plugin/commands/slacker.md` `# Emoji state machine` doctrine section — state→emoji table, transition triggers, override-block format, `/set-reaction` invocation example, lazy-reconcile note.

### Modified

- `plugin/bridge/slack/src/bridge/http-server.ts` — new `POST /set-reaction` route. Body `{channel, ts, state}`. Response `{ok, previous, current}`. Errors: 400 unknown-state, 502 Slack errors, 503 if manager unavailable. `HttpContext` gains `reactionManager: ReactionManager | null`. `startHttpServer` accepts the manager in args.
- `plugin/bridge/slack/src/index.ts` — instantiates `ReactionManager(app.client, process.env.BRIDGE_LEARNINGS_PATH)` at startup and passes it into `startHttpServer`. Always-on (no env-gate); BRIDGE_LEARNINGS_PATH unset = built-in defaults only.
- `plugin/commands/slacker.md` step 5 spawn — sets `BRIDGE_LEARNINGS_PATH=$ROOT/implementations/learnings/slacker.md` env var on all 4 spawn variants alongside the existing `WOW_INTERACTORS_PATH` / `WOW_INTERACTOR_OVERRIDES_PATH` / `BRIDGE_HTTP_PORT` / `BRIDGE_DATA_DIR` / token env vars.
- `plugin/commands/slacker.md` escalation flow (§4 steps 1 + 4) — replaces the legacy `POST /reaction/add thinking_face` / `POST /reaction/remove thinking_face` calls with `POST /set-reaction {state:"thinking"}` and `POST /set-reaction {state:"done"}` to delegate the remove+add atomically via the state machine.
- `plugin/bridge/slack/src/bridge/http-server.ts` `GET /health` endpoint list (in doctrine) — adds `POST /set-reaction` to the catalogue line.

### Consumer action

1. `/reload-plugins`.
2. Restart `S`.

The bridge is fully backwards-compatible: the existing `POST /reaction/add` / `POST /reaction/remove` endpoints are untouched. Only S's escalation flow now uses `/set-reaction` instead of the pair; old learnings invoking the underlying primitives keep working.
