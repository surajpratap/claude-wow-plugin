# `3.22.0` → `3.23.0`

Migration history as per-version files (Story 085, sprint 2026-05-17-version-coherence). The single shared append-only migration table is replaced by one file per version under `migrations/entries/`. **NEW** `plugin/docs/superpowers/migrations/entries/` — each version from `3.23.0` onward is a file `entries/<X.Y.Z>.md` (filename = target version) holding that version's migration prose; parallel branches add independent files (no merge conflict — fixes backlog 067). **FROZEN** `manager-schema-migrations.md` — its 87 historical rows (through v3.21.0) stay as a static block, never appended to again; the two chronologically inverted rows (`3.15.0 → 3.16.0` / `3.16.0 → 3.17.0`) are reordered to ascending; the header is rewritten to describe the split. **MODIFIED** `plugin/commands/_manager-startup.md` — Phase-1 step-9 `ROW_V` now resolves the highest version among `entries/*.md` filenames via `sort -V` (no grep over row prose — fixes the backlog-117 false version-drift); `PJ_V`/`MGR_V` unchanged. **MODIFIED** `plugin/scripts/sprint-merge-bump.sh` — step 5b retargeted from `manager.md` to `_manager-startup.md` (the "Plugin version" literal moved there in v3.10.0; the wrapper had silently stopped stamping it); step 5c removed (the migration-playbook `.version` write is a version-agnostic `<target>` placeholder — nothing to stamp); step 5d now substitutes `3.22.0`/`3.23.0` inside `entries/NEXT-<story-id>.md` and `git mv`s it to `entries/<resolved-version>.md`. **MODIFIED** root `CLAUDE.md`, `plugin/commands/manager.md`, `plugin/commands/senior-developer.md`, `plugin/commands/pair-programmer.md` and the doctrine sweep set — the "add a migration row" doctrine becomes "add a `migrations/entries/<version>.md` file" (stale "table below" / "in `commands/manager.md`" references corrected). **NEW** `plugin/tests/migration-entries-coherence.sh` — asserts step-9's three version sources agree, entry filenames are valid semver or `NEXT-<id>` placeholders, and `sort -V` picks the true highest version. **MODIFIED** `tests/dist-tree-shape.sh` — asserts `migrations/entries/` ships in the dist subtree. **Suite count: 78 → 79.** **Consumer action after upgrade:** `claude plugin update claude-wow`, `/reload-plugins`, restart M. Just update `.version`.
