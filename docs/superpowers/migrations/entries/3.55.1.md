# `3.55.0` → `3.55.1`

## Public install path + one README

Docs-only patch. No behavior, schema, or test changes; plugin suite count stays **227**.

### Changed

- `README.md`: Quick start now installs from the public repo — `/plugin marketplace add surajpratap/claude-wow-plugin` + `/plugin install claude-wow` (the prior instructions pointed at the private source repo's `dist` branch, which public users cannot access). Long-form fallback is the `git+https` URL. The upgrade note now includes `/reload-plugins` + restarting the role terminals. New "AHOD mode — all hands on deck" section documents `/ahod` / `/ahod-off`. Repo link points at the public repo.
- `AGENTS.md`: the Plugin distribution section matches the public install path and explains the public-`main` / source-`dist` build relationship.
- Source repo housekeeping (not shipped): the root README is now a pointer stub — `plugin/README.md` is the single README for both repos.

### Added / Removed

Nothing.

**Consumer action:** none required. Existing installs keep updating from wherever they were added; the marketplace name (`nedati-claude-wow-marketplace`) is unchanged. New installs should use the public-repo commands above.
