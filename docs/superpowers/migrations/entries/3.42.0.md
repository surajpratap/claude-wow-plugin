# `<NEXT-from>` → `<NEXT-to>`

<!-- sprint-mode placeholders — `<NEXT-from>` and `<NEXT-to>` are
     resolved at finalize time by `sprint-merge-bump.sh`. They are
     NOT a missing version bump that this branch should perform —
     flagging them as such is a known external-reviewer false positive. -->

## 170 — Mechanical repeat-N×-under-load gate for timing/poll tests + generous-ceiling lint

The false-FAIL twin of 169's false-PASS guard (FINDING-46: a tight `wait_for`
ceiling false-FAILed ~50% under load, invisible to a 1× run). Two opt-in
mechanical gates:

- **`run-all.sh --repeat-timing[=N]`** (N default 4, clamp 3–5, env
  `WOW_REPEAT_TIMING_N`) re-runs only the timing-flagged subset
  (`grep -lE 'wait_for|sleep|poll'`) N× and FAILS with `FLAKE: <suite> (k/N)`
  when a suite passes some runs and fails/times-out others (`passes>0 && fails>0`).
  Default `run-all.sh` / `--quick` behaviour is unchanged. Catches flakes that
  recur within N consecutive runs — NOT a concurrent-load harness.
- **`lint-timing-ceilings.sh`** floors every `wait_for <file> <pat> <count> <ceiling>`
  ceiling (the LAST positional — quote-safe) at `WOW_MIN_WAIT_CEILING_S`
  (default 30); an inline `# generous` / `# settle` marker opts a call site out.
  Scopes ONLY the `wait_for` ceiling (no bare-`sleep` clause). Exits 0 on the
  real tree (ceilings are 30/40/60s).

### Added
- `plugin/tests/lint-timing-ceilings.sh` + `plugin/tests/repeat-timing-mode.sh` (behavioral, self-hosting `# RED-WITHOUT:`).
- `plugin/tests/fixtures/timing/` (`tight`/`generous`/`marked`/`spaced`) + planted-flake/stable fixtures.
- `plugin/tests/.red-without/{repeat-timing-aggregation,timing-floor}.patch`.

### Modified
- `plugin/tests/run-all-inner.sh` — `--repeat-timing[=N]` mode (timing subset, N× flake gate; default path untouched).
- `plugin/tests/github-bridge-subcall-degraded.sh` — added the `# settle` allow-marker to the intentional settle-sleep.
- `plugin/commands/{tester,senior-developer,pair-programmer}.md` — one line each pointing at the mechanism.

Suite count: **+2 plugin suites** in isolation — two new top-level test files, `repeat-timing-mode.sh` + `lint-timing-ceilings.sh` (the M scoping note estimated +1 at story-creation, before the mode test and the lint were split into two files; the actual delta is +2). M reconciles the standing-doc absolute vs 169 at merge.

### Consumer action
`/reload-plugins` + restart peers. No data migration. `--repeat-timing` is opt-in (CI/merge-gate + T-verify); ordinary runs are unaffected.
