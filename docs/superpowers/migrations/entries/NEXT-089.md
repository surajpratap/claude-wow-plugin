# `<NEXT-from>` → `<NEXT-to>`

Slacker confirms its Slack-bridge channel scope once with M, then remembers it (Story 089, sprint 2026-05-17-slack-bridge-hardening). **MODIFIED** `plugin/commands/slacker.md` — the "## Bridge auto-launch" flow gains step 4c "Resolve channel scope": S reads a `<!-- slacker-channel-scope -->` block from `learnings/slacker.md`; if absent, it asks M (→ human) once via a `question` / `answer` round-trip and persists the decision; the step-5 spawn now has two branches — scoped (`BRIDGE_CHANNEL=<id-or-#name>`) or unscoped (`env -u BRIDGE_CHANNEL`, the `all` case). A new "## Channel-scope learning" section documents the persisted block format. **MODIFIED** `plugin/commands/_slacker-startup.md` — the step-4 auto-launch summary names the new channel-scope sub-step and corrects stale env-var names (`PORT` / `EVENTS_PATH` → `BRIDGE_HTTP_PORT` / `BRIDGE_DATA_DIR`, plus `BRIDGE_CHANNEL`). No bridge code change — the bridge already supports `BRIDGE_CHANNEL` / `--channel`. Bundled bash test-suite count unchanged. **Consumer action after upgrade:** `claude plugin update claude-wow`, `/reload-plugins`, restart Slacker — on its next launch S confirms channel scope once if no `<!-- slacker-channel-scope -->` block exists yet. Just update `.version`.
