# `3.47.0` → `3.48.0`

## 180 — Accuracy-trace convention: claim→authoritative-source map + structural lint

Doc/claim-heavy stories now carry an `## Accuracy-trace map` in the SD plan (`| Claim | Authoritative source | Anchor | Verifier |`), driven by an M-set `<!-- accuracy-trace: required -->` story marker. A new structural lint enforces it mechanically; PP/T split the claim surface to deep-check meaning. Addresses the FINDING-53/54 stale-summary class.

### Added

- `plugin/scripts/accuracy-trace-lint.sh` — structural lint (map present; no summary-doc citation; verifier in {pp,t}; anchor grep-resolves; doc-heavy-unmarked WARN). Runs inside `plan-shape-gate.sh`.
- `plugin/tests/accuracy-trace-lint.sh` — red-green (10 cases).

### Modified

- `plugin/scripts/plan-shape-gate.sh` — also runs `accuracy-trace-lint.sh` on each modified plan.
- `plugin/commands/_agent-protocol.md` — registers the marker + map format (single source of truth).
- `plugin/commands/senior-developer.md` — map template + the plan self-check line.
- `plugin/commands/pair-programmer.md`, `plugin/commands/tester.md` — the verify-split lens; `plugin/commands/manager.md` — sets the marker at story creation.
- `plugin/tests/plan-shape-gate.sh` — adds a wiring case proving the gate invokes the lint.

**Consumer action:** `/reload-plugins`, then restart peers via `/<role>` — running peers retain the old plan-review/verify doctrine until restart. Suite count +1 (new `accuracy-trace-lint.sh`).
