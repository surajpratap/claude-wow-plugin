# `3.43.0` → `3.44.0`

## 176 — Dependency version policy (track latest) + `--deps` routine drift check

Codifies an existing-but-undocumented policy: the six `claude-plugins-official`
dependencies declared in `plugin.json` intentionally track **latest** (no
`version` pins). The breaking-change risk is accepted and mitigated by a routine
drift check rather than by pinning.

### Added

- `check-plugin-updates.sh --deps` — offline (jq-only) mode that lists each
  declared dependency as `dep <name> <marketplace> tracks-latest`, surfaces the
  `claude plugin list` review command, and exits non-zero with a
  `pinned <name> <version>` diagnostic if any dependency ever gains a `version`
  pin.
- `plugin/tests/deps-track-latest.sh` — suite-enforced no-pin guard (+1 suite).

### Modified

- `plugin/AGENTS.md` + `plugin/README.md` — explicit dependency-version policy
  note; README dependency count corrected (two → six).

**Consumer action:** none. Documentation + a new mechanical guard only; no
runtime behavior change.
