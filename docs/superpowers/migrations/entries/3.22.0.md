# 3.21.0 → 3.22.0

Retire the `expected_suite_count` `story-done` payload field (Story 086, sprint 2026-05-17-version-coherence). The field was a pre-declared snapshot of the same `tests/*.sh` glob `run-all.sh` auto-discovers — it could not catch a dropped suite and its only real failure mode was staggered-merge staleness (backlog 074). **REMOVED** the `expected_suite_count` clause from the `story-done` row in `plugin/commands/_agent-protocol.md`; the SD instruction block in `plugin/commands/senior-developer.md`; the T assertion block in `plugin/commands/tester.md` (replaced by one line — a "new test suite" AC is verified by confirming the AC-named file exists + runs green); the `expected_suite_count` mechanism in `implementations/learnings/tester.md` (the hardcoded suite-count totals there were also genericized). **MODIFIED** `plugin/tests/run-all.sh` AND root `tests/run-all.sh` (byte-identical) — a full-mode structural self-check: the SET of suites executed must equal the SET of `tests/*.sh` files present; reported in the summary; non-zero exit on a set mismatch; `--quick` is exempt. **NEW** `plugin/tests/run-all-self-check.sh` — covers the self-check (positive + sabotaged-discovery negative case). **Suite count: 77 → 78.** **Consumer action after upgrade:** `claude plugin update claude-wow`, `/reload-plugins`, restart peers. Just update `.version`.
