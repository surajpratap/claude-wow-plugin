# 3.24.3 → 3.24.4

## Removed

- **The user-attention sound mechanism — removed entirely (Story 130).** The `wow-attention` player, the bundled `attention.wav` doorbell, and M's "Attention signal" doctrine (shipped across stories 081–084) are gone. The human decided the mechanism doesn't work well; a future attention mechanism will be designed fresh. Deleted:
  - `plugin/bin/wow-attention` (the player). `plugin/bin/` retains `wow-locate`.
  - `plugin/assets/attention.wav` (the bundled doorbell). The `plugin/assets/` directory is now empty and no longer in the tree.
  - `plugin/tests/attention-sound.sh` (the Story-084 removal-regression guard, now obsolete).
  - `scripts/gen-attention-sound.py` (source-repo-only WAV generator) + `docs/superpowers/specs/2026-05-16-protocol-discovery-and-attention-sound-design.md` + `docs/superpowers/specs/2026-05-16-attention-sound/` (design specs — source-repo-only, not bundled).
- The `**Attention signal.**` paragraph removed from `plugin/commands/manager.md`.

## Modified

- Root `AGENTS.md` — the `scripts/` layout line no longer names `gen-attention-sound.py`; `release-dist.sh` is the sole source-repo-only tool.
- `implementations/learnings/{tester,pair-programmer,senior-developer}.md` — attention-sound-specific examples pruned; the generic craft lessons they carried (stub-binary testing, hook path-geometry, TOCTOU calibration, subtraction-story review pattern, mechanical-over-prose precondition, generator fidelity, etc.) were preserved and genericized.

## Suite count

Plugin suite: 100 → 99 (`attention-sound.sh` removed). Root suite unchanged.

## Consumer action

`/reload-plugins` + restart peers per standard upgrade hygiene. No behavior change beyond the removal — M no longer plays a sound; nothing replaces it yet.

## Migration history note

The mechanism is still named in the frozen `manager-schema-migrations.md` rows (`3.18.1→3.19.0`, `3.19.0→3.20.0`, `3.20.0→3.21.0`) — those are the canonical changelog and stay verbatim. This entry is the record of the mechanism's removal.
