# `<NEXT-from>` → `<NEXT-to>`

Three retro-flow / sprint bus types added to the MCP server's allow-list (Story 087, sprint 2026-05-17-slack-bridge-hardening). **MODIFIED** `plugin/mcp/claude-wow-server/server.py` — `ALLOWED_TYPES` gains `sprint-ack`, `retro-opening`, `retro-close`; peers can now emit these directly via `bus_emit` instead of the `status` + `intent:` workaround that cluttered audit / retro tooling. (`learnings-updated`, `retro-learnings-window-open`, `review-closed` — also retro-flow types — were already in the enum; the story's original "six" over-counted.) **MODIFIED** `plugin/commands/_agent-protocol.md` — the message-type reference table gains a descriptive row for each of the 3 new types, keeping the protocol spec (the "single source of truth") consistent with the enum. This is a doc-sync for types already used as doctrine by `manager.md` (Step 2) and `_retro-doctrine.md` (Steps 1 + 4) — not a behavioral change. **MODIFIED** `plugin/tests/mcp-server-bus-emit.sh` — a positive case (case 9) asserting each of the 3 new types emits cleanly with no enum rejection. Bundled bash test-suite count unchanged. **Consumer action after upgrade:** `claude plugin update claude-wow`, `/reload-plugins`, restart peers. Just update `.version`.
