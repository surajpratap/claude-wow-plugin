# `3.48.0` → `3.49.0`

## 181 — i_am_truly_idle handshake + gated declare_idle + M-never-ignores-nudge

An affirmative idle handshake gives M ground truth: peers call the new `i_am_truly_idle` MCP tool when genuinely idle; `declare_idle` is gated on all of sd+pp+t being confirmed-idle + pid-alive + quiet (refuses-and-names otherwise); `resume_work` clears both markers. M never ignores an idle nudge — declare-or-nudge.

### Added

- `i_am_truly_idle` MCP tool (`mcp/claude-wow-server/server.py`) — records `implementations/.truly-idle.json` `{role:{idle,ts,pid}}` (idempotent per role; pid sourced from the role's most-recent `.activity.jsonl` `claude_pid`).
- `plugin/tests/mcp-idle-handshake.sh` — 10-case red-green (handshake, gate refuse/pass, dead-pid, post-idle work, dual-clear) + `.red-without/181-declare-idle-gate.patch`.

### Modified

- `server.py` — `declare_idle` gated (sd+pp+t confirmed + pid-alive + no work-shaped `.activity.jsonl` row since the idle mark; `stop`/`stop_failure`/`session_end` excluded); `resume_work` dual-clears `.nothing_to_do` + `.truly-idle.json`; CLI shims for the idle tools.
- `scripts/wow-process/idle-monitor.py` — `all-idle-nudge` payload gains per-role `idle_status` (role + confirmed + ts).
- `commands/manager.md` — idle-monitor handler is declare-or-nudge (never absorb). `commands/_agent-protocol.md` registers the tool + `.truly-idle.json` schema + gated-declare contract (SSOT). `commands/{senior-developer,pair-programmer,tester}.md` — call `i_am_truly_idle` on idle/`wake`.
- `plugin/tests/declare-idle-tool.sh` — confirms sd+pp+t before `declare_idle` (satisfies the gate).

**Consumer action:** `/reload-plugins`, then restart peers via `/<role>` — the MCP server gains the new tool + gating; running peers retain old doctrine until restart. Suite count +1 (`mcp-idle-handshake.sh`).
