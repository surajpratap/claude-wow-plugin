# `<NEXT-from>` → `<NEXT-to>`

Slacker bridge-PID handling scoped strictly per-project (Story 093, sprint 2026-05-17-slack-bridge-hardening). Multiple projects can each run their own Slacker + Slack bridge on one machine; S must never signal or mis-detect another project's bridge (backlog 132). **NEW** a "## Bridge ownership check" section in `plugin/commands/slacker.md` — before S signals/restarts/flags any bridge process it confirms ownership: the PID from this project's `.bridge-pid` is alive AND `GET /health` on `$PORT` reports this project's `eventsPath`. **MODIFIED** `plugin/commands/slacker.md` — the "## SIGUSR1 re-arm parity" section now gates `kill -USR1` on the ownership check (a stale or PID-reused process is never signalled); the pre-spawn collision check resolves a port holder as this project's own stale bridge vs a foreign process and never touches a foreign one. No process-name (`pkill`) sweep is used anywhere. Audit (in the story plan) confirms every other bridge-PID path was already project-local. No code change — doctrine only. Bundled bash test-suite count unchanged. **Consumer action after upgrade:** `claude plugin update claude-wow`, `/reload-plugins`, restart Slacker. Just update `.version`.
