# `<NEXT-from>` → `<NEXT-to>`

Slack-bridge setup documentation added (Story 088, sprint 2026-05-17-slack-bridge-hardening). **NEW** `plugin/bridge/slack/README.md` — documents the Slack-app configuration the bundled bridge requires: the app-level token scope (`connections:write`), the full required bot-token OAuth scope set, the optional `*:history` scopes and the condition each applies under, the Event Subscriptions, and a traceability table of every Slack Web API method the bridge calls (verified against `bridge/slack/src/` at write time). The required scope set reflects story 090 — `channelByName` no longer enumerates `mpim` for by-name resolution; `mpim:read` is documented as still required via `users.conversations` (the `/conversations` endpoint), with that distinction called out. Documentation only — no code change. Bundled bash test-suite count unchanged. **Consumer action after upgrade:** `claude plugin update claude-wow`, `/reload-plugins`; no peer restart required (doc-only). Just update `.version`.
