# `<NEXT-from>` → `<NEXT-to>`

Slack events-feed trim `ts`/cutoff format mismatch fixed (Story 094, sprint 2026-05-17-slack-bridge-hardening). The 1-week `events.jsonl` trim computed its `cutoff` as an ISO-8601 string but the feed's `ts` is the raw Slack message timestamp — a Unix-epoch decimal string — so the `jq` string comparison was meaningless (backlog 133): latent until the feed passed ~2000 lines, then it would either never trim or wipe live history. **NEW** `plugin/scripts/slack-events-trim.sh` — the trim logic extracted from the `trim_events_feed` doctrine helper into a unit-testable script (mechanical-over-prose); the `cutoff` is now a Unix epoch (`date +%s`) and the `jq` comparison is numeric (`(.ts | tonumber?) >= $cutoff`), with a fail-safe cutoff pre-check and a load-bearing `jq … && mv` guard so a `jq` failure can never overwrite the live feed. The feed's `ts` is left Slack-native — it doubles as the Slack message identifier (`thread_ts` / edit / delete). **MODIFIED** `plugin/commands/slacker.md` — the "## Events-feed trim helper" inline bash function is replaced by a pointer + two `wow-locate` script invocations (startup step 7b + every-100th-tick); the stale "ISO-8601 `ts`" comment corrected. **MODIFIED** `plugin/tests/slack-events-trim.sh` — drives the real script; fixtures emit Unix-epoch `ts` (the old ISO-8601 fixtures masked the bug). **MODIFIED** root `AGENTS.md` + `plugin/AGENTS.md` — the doctrine-invoked helper-script count is now 12 (`slack-events-trim.sh` added). Bundled bash test-suite count unchanged (the `slack-events-trim.sh` test already existed). **Consumer action after upgrade:** `claude plugin update claude-wow`, `/reload-plugins`, restart Slacker. Just update `.version`.
