# `<NEXT-from>` → `<NEXT-to>`

Slack bridge `channelByName()` stops enumerating multi-person DMs (Story 090, sprint 2026-05-17-slack-bridge-hardening). **MODIFIED** `plugin/bridge/slack/src/bridge/cache.ts` — `channelByName()`'s `conversations.list` call drops `mpim` from its `types` filter (now `public_channel,private_channel`). A multi-person DM has no `#name`, so it can never be a valid by-name resolution target; the `mpim` type was dead weight and also forced the bot token to carry an otherwise-pointless `mpim:read` OAuth scope solely for this enumeration. **NEW** a `node:test` case in `plugin/bridge/slack/tests/smoke.test.ts` covering `channelByName()` public + private by-name resolution and asserting `mpim` is absent from the `conversations.list` `types` filter. The bundled bash test-suite count (`tests/run-all.sh`) is unchanged — the new test is a Slack-bridge `node:test` case, not a bash suite. **Consumer action after upgrade:** `claude plugin update claude-wow`, `/reload-plugins`, restart peers. Just update `.version`.
