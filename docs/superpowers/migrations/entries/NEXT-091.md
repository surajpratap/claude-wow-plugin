# `<NEXT-from>` → `<NEXT-to>`

Slacker's Slack-bridge health-check cron removed (Story 091, sprint 2026-05-17-slack-bridge-hardening). **REMOVED** the 5-minute `/health`-poll `CronCreate` from `plugin/commands/_slacker-startup.md` (startup step 10), the matching `CronDelete` from `plugin/commands/slacker.md`'s "On clean exit", and the "health-check cron ID" item from the startup "Tell the human" step. **MODIFIED** `plugin/commands/slacker.md`'s "# Bridge health monitoring" section — rewritten around the bridge's existing persistent spawn-`Monitor` task: S now treats the Monitor task ending (process death) and the bridge's `socket-mode → disconnected` / `socket-mode → failed` stdout lines (streamed as Monitor events) as the health signal, escalating to M with the same `question` shape. No polling cron. No bridge code change. Bundled bash test-suite count unchanged. **Consumer action after upgrade:** `claude plugin update claude-wow`, `/reload-plugins`, restart Slacker — its next startup arms no health-check cron and reads the Monitor-event health doctrine. Just update `.version`.
