<!--
LLM-INSTRUCTION DIRECTIVE — READ ON-DEMAND ONLY, FORGET AFTER USE

This file is the canonical changelog of WOW schema migrations for `commands/manager.md`. It is intentionally kept OUT of the role file's normal session-load surface to keep `commands/manager.md` lean.

When to read this file (M only):
- M is performing the Phase 1 migration playbook AND `.version` differs from target — read the row(s) for the from→target transition (or the cumulative path), apply, commit. Then drop the content from working context.
- A bug or PR review explicitly references a historical version transition — read the relevant row(s), use the context, then drop.
- Backlog grooming on a topic that touches schema history — same pattern.

When NOT to read this file:
- Routine session start. M's normal loop does not need migration history.
- Routine PP / SD / T loops. Peers do not consume this file at all.
- Searching for "current behavior" — current behavior lives in the body of `commands/manager.md`, NOT here. Migration rows are append-only history; they may reference paths/scripts/tools that were deleted in subsequent rows. Do not treat any row as describing CURRENT state.

Forgetting discipline: after applying a migration or answering the specific question that brought you here, do NOT carry the whole table forward in working context. Re-read on-demand when needed again. The content is large; carrying it indefinitely bloats sessions for no benefit.

Append-only rule: rows are added only; never edited or deleted. New stories add a single new row at the bottom describing the from→target transition. Stories that supersede earlier work (e.g., delete a script introduced in a prior row) annotate within their OWN row, not by editing the prior row.

Authority: M owns this file. SD touches it only as part of an approved story (e.g., adding the row for a story that bumps the version). PP reviews additions during plan-review and post-impl review (atomicity does NOT apply — this file is the changelog by design, not a current-state directive).
-->

# Manager schema migrations

Canonical changelog of WOW schema transitions. Read on-demand only (see directive above). The table below is the authoritative `From → To` transform list referenced by `commands/manager.md` Phase 1 step 4 ("Migration playbook").


| From → To              | Steps                                                                                                                                                                                                                                                                                                                             |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| any `< 2.0.0` → `2.1.0` | `rm -rf "${ROOT}/implementations/buses/"` (old hub-spoke pair-buses). `touch "${ROOT}/implementations/.message-bus.jsonl"` (if missing). `rm -f "${ROOT}/implementations/.agents/"*.json` (offset-tracker shape changed — peers regenerate on next start). Leave stories, plans, bugs, tests-stories, backlog, learnings intact. |
| `2.0.0` → `2.1.0` | No schema transforms. The shared bus-tail filter script is picked up automatically from the plugin cache on peers' next Monitor arm; no on-disk change in the project tree. Just update `.version`. |
| `2.1.0` → `2.2.0` | No schema transforms. New optional file `${ROOT}/implementations/.bus-trim-threshold` (single integer, default 2000 if absent) tunes when M's trim runs. Per-agent bus-tail cursors at `${ROOT}/implementations/.agents/<agent-id>.bus-tail-cursor` are created automatically by the rewritten `bus-tail.sh` on next Monitor arm. Just update `.version`. |
| `2.2.0` → `2.3.0` | `mkdir -p "${ROOT}/implementations/.github/"` (runtime state dir for the new GitHub bridge introduced in v2.3.0). The bridge itself spawns at the next M startup post-migration, gated on the presence of `.github/config.json`; the migration just creates the directory. Just update `.version`. |
| `2.3.0` → `2.4.0` | No schema transforms. v2.4.0 extends the bridge with `pr-review` and `pr-comment` events, adds M-side burst-collapse for rapid-fire comments via `ScheduleWakeup(60s)`, and grants PP a new `gh pr comment` authority for replying on watched PRs. All changes live in agent prompts and the bridge binary; no on-disk project files change. Per-PR cursors gain `last_review_id` / `last_comment_id` fields populated lazily on first observation (no replay). Just update `.version`. |
| `2.4.0` → `2.5.0` | No schema transforms. v2.5.0 closes the GH-watch arc: bridge gains the `ci-check` event type (cursor adds a `last_seen_check_states` dict per PR — populated lazily, no replay), adds optional webhook mode via the `cli/gh-webhook` extension (config `mode: "webhook"` + admin on each repo; falls back to polling-only if the extension or admin is missing), and explicit multi-repo support (config `repos` is a list; each repo gets its own forwarder + cursor). PP gains a CI-failure triage subsection (real-bug / env-flake / test-update outcomes). All changes live in agent prompts, the bridge binary, and optionally `${ROOT}/implementations/.github/config.json` (new optional fields `mode`, `webhook_safety_net_interval_sec`, `webhook_forwarder_restart_backoff_sec` are backward-compat — older configs continue to work as polling-only). Just update `.version`. |
| `2.5.0` → `2.6.0` | No schema transforms. PP/T's fswatch monitor picks up the expanded baseline (`.github/` exclude added) on next arm; existing sessions can re-arm or wait until they next restart. M's prompt also gains explicit Phase 1 detection of the canonical branch (`${CANONICAL_BRANCH}` via `git symbolic-ref`) and a prominent rule that backlog files commit on the canonical branch — branch-name-agnostic (covers `main` / `master` / `trunk` / `develop`). Documentation-only for the canonical-branch piece; no on-disk schema changes. Just update `.version`. |
| `2.6.0` → `2.7.0` | No schema transforms. Existing per-PR cursors auto-migrate on first read (old `last_comment_id` is copied into both new `last_issue_comment_id` and `last_review_comment_id`, then removed). Just update `.version`. |
| `2.7.0` → `2.8.0` | No schema transforms. M's Phase 1 gains an auto-cleanup step for stale merged feat-branches (`feat/<NNN>-*` reachable from canonical, tip > 3d old, worktree clean). Existing sessions pick it up on next restart. Just update `.version`. |
| `2.8.0` → `2.9.0` | No schema transforms. Bridge gains re-arm-from-polling-only path + forwarder stderr capture; M gains user-presence re-arm trigger and a new `github_bridge_state` field in its offset tracker. Existing sessions pick up on next restart. Just update `.version`. |
| `2.9.0` → `2.10.0` | No schema transforms. Sprint mode added; existing per-story flow unchanged. New `implementations/sprints/` directory created lazily on first sprint. Bus messages gain optional `sprint_id` and `item_id` fields. Existing sessions pick up on next restart. Just update `.version`. |
| `2.10.0` → `2.11.0` | No schema transforms. M's "Asking the human questions" prose tightened — the open-ended-question exception is removed; every M→human question now goes through `AskUserQuestion` (the tool's built-in free-text "Other" answer covers cases that resist enumeration). New "Decide-and-report alternative" subsection added under "Interactive behavior". Anti-pattern callout added under "Standing authority". Just update `.version`. |
| `2.11.0` → `2.12.0` | No schema transforms. M's offset-tracker JSON gains `last_user_prompt_ts` and `auto_promote_paused_until` fields — both auto-init to `null` on first read; existing tracker files don't need an explicit transform. Backlog files include `<!-- concern: -->` and `<!-- size: -->` markers (retro-filled across all existing items in the same commit); M's autonomous-pickup gate consumes them. New `<user-prompt-submit-hook>` Monitor handler updates the tracker timestamp and runs the disapproval-brake matcher. Just update `.version`. |
| `2.12.0` → `2.13.0` | No schema transforms. M's offset-tracker JSON gains `cron_cadence` (auto-init `"fast"`), `last_liveness_round_ts` (auto-init `null`), and `last_liveness_round_results` (auto-init `null`) fields — existing tracker files don't need an explicit transform. The passive idle-off trigger is replaced by an active pre-sleep liveness round: at the 10-quiet-tick threshold M pings each core peer and only sleeps if all three pong; otherwise enters a 30-min slow-cron fallback and escalates via `AskUserQuestion`. Auto-recovers to fast-cron when the missing peer comes back. The autonomy-pickup gate's "Team idle" check now consumes the shared `last_liveness_round_*` cache (consumes the freshness rule defined on the schema row). Existing sessions pick up on next restart. Just update `.version`. |
| `2.13.0` → `2.14.0` | No project-side schema transforms. New helper `scripts/wow-storage.sh` and `~/.wow-kindflow/` home-dir storage convention. M's prompt gains a `Home-dir storage` top-level section + `Cred bootstrap` sub-section under Interactive behavior. The home-dir migration playbook is independent: `wow_storage_init` writes `~/.wow-kindflow/.version = 1.0.0` on first invocation; subsequent calls are no-ops. Existing sessions pick up on next restart. Just update `.version`. |
| `2.14.0` → `2.15.0` | No schema transforms. `tests/run-all.sh` gains `--quick` flag for in-sprint verification: scopes to suites mentioning files changed since HEAD~1, plus `version-coherence` (always); falls back to a full run when no changes are detected. Default behavior (no flag) unchanged. Existing sessions pick up on next restart. Just update `.version`. |
| `2.16.0` → `2.17.0` | Documentation-only. The `last_liveness_round_*` cache freshness rule is now defined ONCE on the Phase 3 step 2 tracker-schema row for `last_liveness_round_ts` (canonical "**Freshness rule**" annotation carries the literal duration). Consumer prose (Cron-lifecycle Autonomy-pickup Team-idle bullet, migration-row history) references the schema row by name instead of restating the literal. No behavior change. Just update `.version`. |
| `2.17.0` → `2.18.0` | New bundled Slack bridge at `bridge/slack/` (TypeScript Bolt+Socket-Mode + Web API + dotenv; source copied from `nedati-technologies/slack-bridge`). S's startup now auto-launches it instead of requiring an external `claude-slack-bridge` process. Cred map at `~/.wow-kindflow/slack/<project-key>/creds.json` per v2.14.0 home-dir convention; cred shape is `{bot_token, app_token, schema_version: "1.0.0"}`. Existing legacy `<!-- slacker-bridge-config -->` blocks in slacker.md learnings are ignored by the new flow but harmless. First S start runs `npm ci` in `bridge/slack/` (~30–60s); subsequent starts skip via `.deps-installed` SHA cache. Pre-approved env-dep additions: `node >= 20`. Just update `.version`. |
| `2.19.0` → `2.20.0` | Sprint mode behavior change. Stacked-child branch + worktree creation deferred from sprint kickoff to the moment M observes the parent's `plan-approved` event. Manifest schema gains optional `item.plan_approved_at: ISO|null` field (auto-init `null`); `scripts/sprint-graph-next-dispatchable.sh` uses it to gate stacked-child dispatchability. Eliminates the version-literal cascade-conflict class identified in sprint 2026-05-01 retro. M's prompt gains a "Reacting to `plan-approved` (sprint mode)" Monitor handler under "Reacting to Monitor events". Existing in-flight sprints' manifests don't need transform — `plan_approved_at` defaults to `null` and the new behavior takes effect on next dispatch cycle. Just update `.version`. |
| `2.20.0` → `2.21.0` | New bus message type `review-closed` (PP → manager-*). M's Phase 4 retro-open trigger now waits for `review-closed` from each active reviewer (PP) before firing. Fallback at 5 min after all-items-terminal prevents indefinite block. M's offset-tracker schema gains `last_all_terminal_ts: ISO|null` (auto-init `null`), `reviewers_closed: [<role>...]` (auto-init `[]`), and `retro_open_fired: bool` (auto-init `false`) for the trigger + idempotency state. PP's prompt gains a "Sprint review-closed signal" subsection. Existing in-flight sprints will see retro-open fire either on PP's first review-closed emit or 5 min after their last item turns terminal — no manifest transform needed. Just update `.version`. |
| `2.21.0` → `2.22.0` | New bus message type `bus-restored` (M / helper → *). `scripts/bus-tail.sh` consumer fast-forwards the per-agent cursor to `payload.current_line_count` past the restoration without emitting events for the gap. New `scripts/wow-bus-restore.sh` helper for ad-hoc user-driven restoration (auto-detects whether M is alive). M's prompt gains a "Bus restoration handshake" subsection under Cron lifecycle. Backward-compatible: bus-tail.sh consumers older than v2.22.0 still emit the line and move cursor normally; the fast-forward is additive. Just update `.version`. |
| `2.22.0` → `2.23.0` | New `/afk` and `/back` slash commands at `commands/_meta/`. M's prompt gains an "AFK handling" top-level section covering idle-AFK (cron teardown) and Leader-AFK (autonomous decisions + audit log) modes. M's offset-tracker JSON gains `afk_active` (auto-init `false`), `afk_mode` (auto-init `null`), `afk_started_ts` (auto-init `null`), `leader_decisions` (auto-init `[]`), `last_afk_session_id` (auto-init `null`). New bus message types `human-afk`, `human-back`, `leader-decision`. `.gitignore` adds `implementations/.afk/` (session-local audit-log mirror dir). 8 design calls were M-solo; spec Section M lists them for human ratification on return. Existing sessions pick up on next restart. Just update `.version`. |
| `2.23.0` → `2.24.0` | New bus message type `bus-wake-bug` (any peer → manager-*). All four core role files gain a "Spurious wake reporting" subsection: agents emit `bus-wake-bug` when their bus-tail Monitor fires with a stale or wrong-addressee line. M aggregates in `bus_wake_bugs[]` (auto-init `[]`); fires a digest via `AskUserQuestion` every 10 reports OR daily (24h since last digest). Tracker also gains `last_bus_wake_bug_digest_ts` (auto-init `null`). Existing sessions pick up on next restart. Just update `.version`. |
| `2.24.0` → `2.24.1` | `commands/slacker.md` Bridge auto-launch flow gains step 4b: when `dist/` missing OR `package-lock.json` SHA changed, run `npm run build` before spawn. Spawn-fail behavior gains `npm run build failed` to the cause list. New `tests/slacker-bridge-launch.sh`. Real bug: Story 017 (v2.18.0) bundled the bridge but never built TS — fresh installs failed at spawn time. Existing sessions pick up on next Slacker restart. Just update `.version`. |
| `2.24.1` → `2.24.2` | New helper `scripts/file-story-from-backlog.sh` for atomic backlog-promotion + story-creation (refuses if backlog status != `accepted` or story file already exists). M's story-filing flow updated to use the helper for backlog-derived stories. Phase 1 startup gains coherence check (scan backlog for `accepted` items with matching filed stories → AskUserQuestion auto-promote). Sprint Phase 1 inventory uses same check on candidate backlog items. Existing sessions pick up on next M start. Just update `.version`. |
| `2.24.2` → `2.25.0` | New helper `scripts/sprint-merge-bump.sh` (Section D-style atomic version-stamp + merge wrapper). M's Phase 3 dispatch uses it instead of `gh pr merge` directly for sprint-mode PRs. SD's plan convention (introduced this version) uses `2.24.2` / `2.25.0` placeholders in migration rows + skips touching `.claude-plugin/plugin.json` `version` and the "M targets plugin version" header literal — wrapper substitutes all three at merge time atomically. Eliminates the cascade-rebase version-literal conflict class on stacked branches. M's Phase 1 gains a "Version coherence repair" step (#9) — detects manual-merge bypass leaving `<NEXT` placeholders or 3-way version disagreement on main, offers to re-stamp + commit. PP's prompt gains plan-review + code-review version-literal checks. Existing sessions pick up on next M start. Just update `.version`. |
| `2.25.0` → `2.26.0` | `story-done` payload gains optional `role_files_updated: [<commands/*.md>...]` array — SD includes paths of role files modified by impl. PP and T gain a "Re-read your role file when flagged" subsection: at session start, scan bus story-done messages since `last_session_ts`; if self-role file appears in any role_files_updated list, log a re-read acknowledgement (re-read happens automatically via Claude Code prompt-load each session). PP/T offset-tracker JSON gains `last_session_ts` field (auto-init `null`). New test `tests/story-done-role-files-updated.sh` covers the consumer-side jq filter. SD's prompt updated with computation guidance (`git diff --name-only` against merge-base, filter to `commands/*.md`). Schema documented in `commands/_agent-protocol.md` story-done row. Existing sessions pick up on next restart. Just update `.version`. |
| `2.26.0` → `2.26.1` | `commands/slacker.md` Bridge auto-launch gains step 7b: opportunistic 1-week truncation of `${ROOT}/implementations/.slack/events.jsonl` (mirrors M's bus-trim pattern). Helper `trim_events_feed` defined inline; runs once at startup (post-`/health`) and again every 100th events-feed Monitor tick (S maintains in-memory counter). Threshold default 2000 lines, overridable via optional `${ROOT}/implementations/.slack/events-trim-threshold` config file. Atomic `.tmp` + `mv` survives `tail -F` inode swap on macOS. New test `tests/slack-events-trim.sh` covers above-threshold trim, below-threshold no-op, missing-file no-op, custom-threshold, malformed-line-drop, idempotent re-run. Existing S sessions pick up on next restart. Just update `.version`. |
| `2.26.1` → `2.26.2` | M's `bus-wake-bug` aggregation digest threshold becomes sprint-mode-aware: when M's tracker `sprint_id` is non-null, threshold drops to **5 reports OR 6h** since last digest (vs steady-state's 10 reports OR 24h). No tracker schema change — derived at evaluation time from existing `sprint_id`. `tests/bus-wake-bug-protocol.sh` extended with `should_fire_digest_sprint_aware` helper + 7 new cases (sprint 5-fire, sprint 4-no-fire, steady 5-no-fire, steady 10-fire, sprint time-fire, steady time-no-fire, sprint time-boundary-fire). Sprint mode runs many parallel agents → high bus volume → tighter threshold surfaces issues mid-sprint. Existing sessions pick up on next M start. Just update `.version`. |
| `2.26.2` → `2.27.0` | `story-created` payload gains optional `in_flight: "<count>/<limit>"` string field — sprint-mode dispatch only. M computes count via `jq` over manifest items in `dispatched` or `in-review` status; limit is `manifest.concurrency_limit // 3`. SD parses the field on receipt and uses it as advisory pacing input — when `count >= limit`, SD finishes current plan + emits `plan-done` before claiming the new story. Schema documented in `commands/_agent-protocol.md` story-created row. SD prompt gains a "Sprint-mode pacing" sub-paragraph under `story-created` handler. New test `tests/sprint-story-created-in-flight.sh` covers M's jq computation across 8 manifest fixtures (empty, 2/3, at-cap, in-review-counts, non-default-limit, default-fallback, completed-excluded, mixed-statuses). No tracker schema change. Existing sessions pick up on next M start. Just update `.version`. |
| `2.27.0` → `2.27.1` | `commands/pair-programmer.md` "Plan / story findings" section gains "Structured AC-count check" subsection: for stories with **more than 5** AC items, the `<!-- reviewer-approval -->` block MUST end with structured count lines (`AC items in story: N` / `AC items addressed in plan: N` / `Counts match: yes`). Replaces prose `"All N AC items covered"` form for those stories. Stories with ≤5 AC items keep prose. Discipline forces enumeration audit; prose-only summaries drift (sprint 2026-05-02-batch retro caught a "4 vs 5 fields" prose error in plan 025). Inline example for a 9-AC story added to the role file. Convention already in practice on recent PP approvals (027, 028, 029, 030); this story formalizes the format. Existing sessions pick up on next PP restart. Just update `.version`. |
| `2.27.1` → `2.27.2` | `Cross-ref:` block formalized as a required field in SD plan template — three lines under `## Notes / constraints` listing source backlog (or "none"), predecessor stories (or "none"), and stacked-on branch (or "none"). `commands/senior-developer.md` plan template gains the block + a "Cross-ref required field" subsection explaining usage. `commands/pair-programmer.md` Plan-review check #3 added: absence of any of the three lines = finding. `commands/tester.md` gains "Cross-ref anchors for spot-checks" subsection: T uses references for spot-check grep anchors + reads predecessors before testing current story. Convention introduced in sprint 2026-05-01 retro, re-confirmed by both T and PP in sprint 2026-05-02-batch retro. Existing sessions pick up on next restart. Just update `.version`. |
| `2.27.2` → `2.27.3` | Documentation-only: clarify the `/reload-plugins` vs running-agent-prompt mismatch. M's Phase 1 step 4 migration playbook gains an "After-migration restart reminder" — when the human approves a migration, M emits a direct text output telling the human to restart running peers (PP/SD/T/Slacker) since `/reload-plugins` only refreshes the plugin cache. New "After a major migration" subsection in `commands/manager.md` explains the cache-vs-in-context-prompt distinction + version-mismatch detection (M compares running agent hello payload to plugin.json version; nudge agent to restart on mismatch). One-line restart hint added to PP/SD/T/Slacker startup sections: if your role file changed in a recent merge, restart yourself. No behavior change beyond the new prose; existing sessions pick up the docs on next restart. Just update `.version`. |
| `2.27.3` → `2.28.0` | T's `story-verified` payload gains optional `humanize_steps: [{step, do, expect}, ...]` array — numbered manual verification steps T cannot automate (UI, external service, plugin runtime, cred bootstrap, migration UX). Schema documented in `commands/_agent-protocol.md` story-verified row. `commands/tester.md` gains "Humanize testing steps" subsection covering when to include + format rules + worked example from the Story 017 build-step incident. `commands/manager.md` story-completion logic gains `humanize_steps` relay paragraph: per-story AskUserQuestion attribution to T in non-sprint mode; sprint-mode aggregates across items into the Phase 4 retro digest, ordered by item_id with `[item NNN]` prefix. Malformed entries surfaced rather than silently dropped. New test `tests/humanize-testing-steps.sh` covers payload parsing, relay decision (present/absent/empty), sprint aggregation ordering, malformed-entry validation, T→M→human routing. Existing sessions pick up on next restart. Just update `.version`. |
| `2.28.0` → `2.28.1` | `commands/senior-developer.md` `plan-approved` handler gains step 2a "Pre-pull main before first edit": when SD claims a sprint-mode story, run `git fetch origin main && git rebase origin/main` BEFORE the first plan/impl edit. Catches stacked-style conflicts at zero-commit state (cheap to resolve). Sprint 2026-05-02-cascade-fix-and-polish retro: SD's mid-027 cherry-pick UU conflict on commands/manager.md cost ~5 min disambiguation; pre-pull would have surfaced at branch entry. Doc-only convention; skip outside sprint mode. Existing sessions pick up on next SD restart. Just update `.version`. |
| `2.28.1` → `2.28.2` | `commands/manager.md` Sprint mode Phase 1 brainstorm gains Step 3a "Spike-first heuristic for foundational stories": when a story's AC introduces a new tool/script the convention depends on AND the same convention applies to other items in the same sprint, M commits a throwaway spike (`docs/superpowers/specs/<topic>/spike.sh`, ~20–50 lines) BEFORE SD writes the plan. Spike is informational only — no PP review, no test, no production use. Surfaces dialect quirks (sed BSD-vs-GNU, regex escape semantics, env-var contracts) before mid-flight amendment cycles hit. Sprint 2026-05-02-cascade-fix-and-polish Story 027 hit 8 amendments (A1–A8) post-plan-approval for lack of one. Existing sessions pick up on next M start. Just update `.version`. |
| `2.28.2` → `2.28.3` | Slacker bridge env-var contract aligned with bundled source. `commands/slacker.md` step 5 spawn now uses `BRIDGE_HTTP_PORT`/`BRIDGE_DATA_DIR` (matching `bridge/slack/src/index.ts` reads), derives `DATA_DIR=$(dirname EVENTS_PATH)`, adds pre-spawn `lsof` collision check (catches stale `claude-slack-bridge` daemons squatting on port). `commands/slacker.md` step 7 `/health` validates returned `port` + `eventsPath` match the requested env-vars; mismatch → degraded with "env-var contract violation" reason. Bridge (`bridge/slack/src/index.ts`) gains 3-line PID-write to `${BRIDGE_DATA_DIR}/.bridge-pid` on startup + unlink on SIGINT/SIGTERM. Bridge `/health` response (`bridge/slack/src/bridge/http-server.ts`) gains `port` + `eventsPath` fields. New test `tests/slacker-bridge-env-vars.sh` (13 assertions / 6 cases) covers spawn-cmd env-var names, DATA_DIR derivation, lsof collision branch, /health mismatch branches, /health match path, PID-path contract. Real bug: discovered via end-to-end Slacker startup post-v2.28.0; bridge silently defaulted to `:3100` + its bundled-source `data/` dir instead of the project-relative path. Existing S sessions pick up on next restart + bridge rebuild (`npm run build`). Just update `.version`. |
| `2.28.3` → `2.28.4` | `commands/senior-developer.md` plan-template section gains "Trivial-tweak plan format" subsection. Eligibility checklist (ALL must hold): ≤5 ACs, no new test file, no new script under `scripts/`, no architectural change. Compressed template (≤30 lines): Context + Implementation order + Verification + Cross-ref; drops Architecture diagram + New/modified files + Process discipline + Verification subsection labels. PP review block + Cross-ref convention preserved. Inline 3-AC example included. Opt-in by SD; PP rejects only clear abuse (e.g., 6-AC story with new test snuck in). Sprint 2026-05-02-cascade-fix-and-polish retro: 031/032/033 plans were ~120 lines for ~50 lines of impl. Existing sessions pick up on next SD restart. Just update `.version`. |
| `2.28.4` → `2.28.5` | Sed safety bundle (defense-in-depth at impl + review time). `commands/senior-developer.md` `# Implementation rules` gains "Sed safety smoke test" bullet: 30-second `sed -E ... <<<fixture` round-trip on macOS BSD before commit; explicitly catches A7 (backticks-in-double-quoted patterns trigger bash command-substitution → silent regex eat) and A8 (BSD sed BRE doesn't grok `\+` → silent no-match). `commands/pair-programmer.md` Code-review version-literal check section gains 2 sed-safety sub-bullets (A7 backticks-in-double-quotes finding, A8 `\+`-without-`-E` finding) with workaround suggestions. Both source from sprint 2026-05-02-cascade-fix-and-polish Story 027 amendment cycle (A7 + A8 required coherence-repair commits 3bb0875 + 6c0022c on main). Existing sessions pick up on next restart. Just update `.version`. |
| `2.28.5` → `2.29.0` | `story-done` payload gains optional `expected_suite_count: <int>` field — exact suite count `bash tests/run-all.sh` should report after this story merges. SD includes when impl modifies the test bench (new `tests/*.sh` file or new asserts). T asserts post-merge count matches; mismatch → file bug. Absence → existing inference behavior (back-compat). Schema documented in `commands/_agent-protocol.md` story-done row. SD's "Story complete" gains paragraph + `ls tests/*.sh \| wc -l` derivation guidance. T's story-done handler gains assertion sub-bullet with bash snippet + back-compat fallback. T retro action item from sprint 2026-05-02-cascade-fix-and-polish: staggered-merge sprints make inference fragile. Existing sessions pick up on next restart. Just update `.version`. |
| `2.29.0` → `2.30.0` | New bus message type `pp-checkpoint` (PP → manager-*). PP emits at sprint-mode item boundaries (after observing each `story-shipped` or `pr-state: merged`). Payload: `{items_reviewed_so_far: [<id>...], open_reviews_now: [<id>...], last_finding_count_per_item: {<id>: <count>}, bus_cursor_line_number_observed: <int>}`. M's offset-tracker schema gains `pp_checkpoints: [...]` ring buffer (auto-init `[]`, last 10). M's `pp-checkpoint` handler appends + trims via jq. PP's role file gains "Sprint-mode checkpoint emission" subsection. Compaction-resilience: PP's next session start (post-compaction or post-restart) seeds reconstruction from the most recent entry. Sprint-mode-only (overhead not worth it elsewhere). Existing in-flight sprints pick up on next PP + M restart — no manifest transform needed. Just update `.version`. |
| `2.30.0` → `2.31.0` | Sprint-end learnings.md staleness trigger: M's Phase 4 retro flow gains Step 5b "Per-peer learnings refresh" between retro.md synthesis (Step 5) and action-items-to-backlog (Step 6). M emits `retro-learnings-window-open` to `*` with `{sprint_id, deadline_ts: now+2min}`; each peer (PP/SD/T/S) skims own `implementations/learnings/<role>.md` for stale facts (suite counts, version refs, deprecated conventions); peer emits `learnings-updated` to `manager-*` with `{path, sha_before, sha_after, summary}` if updated, or stays silent if nothing stale. M waits 2 min then aggregates per-peer count into the retro digest. Soft — no block on missing emits. New bus types `retro-learnings-window-open` + `learnings-updated` documented in `commands/_agent-protocol.md`. Each of 4 peer files gains a "Sprint-end learnings refresh" subsection with full bash emit example. New test `tests/learnings-staleness-trigger.sh` covers payload shape, no-op skip, M aggregation (per-peer + missing-peers-default-zero), window-open shape, deadline-2-min tolerance. T's retro action item from sprint 2026-05-02-cascade-fix-and-polish — tester.md learnings drifted "32 suites at v2.24.0" while sprint shipped at "39 suites at v2.28.0". Existing in-flight sprints pick up on next M + peer restart. Just update `.version`. |
| `2.31.0` → `2.31.1` | Peers (PP/SD/T/S) NEVER call `AskUserQuestion` — complement to Story 013 (M ALWAYS uses it). Each of 4 peer files gains a "Human-routing — hard rule" subsection: emit `question` (or `skill-question` per Story 046) to `manager-*`; M relays via `AskUserQuestion`; M's `answer` returns the human response. Applies even when invoking superpowers skills — peer's prohibition overrides skill's question-asking instruction (route through `ask_via_relay` from Story 046 OR hand-translate the skill's intended question). New lint test `tests/peers-never-askuserquestion.sh` (6 cases): scans each peer file's AskUserQuestion mentions, classifies allowed (prohibition / M-behavior-description / UX-target prose) vs disallowed (un-contextualized peer-instruction). Synthetic positive case + M-out-of-scope assertion. Encodes the de-facto rule explicitly so it survives drift; unlocks Story 046's belt-and-suspenders payoff. Existing sessions pick up on next peer restart. Just update `.version`. |
| `2.31.1` → `2.32.0` | `/superpowers` skills become first-class for PP/SD/T (Path C — prompt-level override, no bash wrapper). Two new bus message types in `commands/_agent-protocol.md`: `skill-question` (peer → manager-*; payload `{question_id, skill, question, options, context_excerpt}`) and `skill-answer` (M → originating peer; payload `{answer, in_reply_to}`). `commands/manager.md` gains "Skill-question relay" subsection: M builds AskUserQuestion call from payload's question + options with attribution `from <peer-role> via skill <skill-name>`, emits skill-answer back. Each of 3 peer files (PP/SD/T) gains "Using superpowers skills" subsection with pre-approved per-role list (PP: requesting-code-review/receiving-code-review/verification-before-completion; SD: writing-plans/test-driven-development/systematic-debugging/executing-plans/frontend-design; T: test-driven-development/systematic-debugging/verification-before-completion) + override prose + bash bus-emit example + 5-step block-poll-resume flow. Same pattern M uses for `superpowers:brainstorming`. New test `tests/superpowers-relay.sh` (7 cases / 17 assertions): payload shape, in_reply_to correlation, non-matching-ignore, M attribution, timeout, multi-question isolation, per-role skill counts. Existing sessions pick up on next restart. Just update `.version`. |
| `2.32.0` → `2.33.0` | New helper `scripts/whats-my-role.sh` — central role-identification primitive for hooks + role-aware code paths. Implements PPID-walk to find claude session PID (skip depth 0 = our bash; match binary basename `claude`/`*/claude` at depth ≥1, walk up to 25 levels, fail-closed), marker file under `.claude/.session-role-by-claude-pid/<pid>` containing role token, mode 0644. Public API: `wow_claim_role <role>` (idempotent on same role; exit 2 on conflict), `wow_whats_my_role` (echoes role; exit 1 if missing/malformed), `wow_release_role` (best-effort), `wow_sweep_stale_role_markers` (M Phase 1 cleanup — drops markers whose claude PID is no longer in `ps`). Sourceable + CLI-invocable. New test `tests/whats-my-role.sh` (13 cases / 15 assertions including ps-shim PPID-walk scenarios for 1-hop, multi-hop, no-claude-fail-closed, marker mode 0644, sweep, conflict, malformed). `.gitignore` adds `/.claude/.session-role-by-claude-pid/` (transient session state, never committed). Spike-validated mechanism per `docs/superpowers/specs/2026-05-02-048-ppid-walk-spike.md`. Foundational primitive — Story 048 hook + future hooks (backlog 055 surveys 11+) consume this API. Existing sessions pick up on next restart. Just update `.version`. |
| `2.33.0` → `2.33.1` | E2E validation for `/superpowers` skill relay (Story 046 protocol). New `tests/superpowers-e2e.sh` (9 cases / 12 assertions) covers full round-trip (peer emit → M relay → answer → resumption), per-role attribution (PP/SD/T/S), 3 concurrent peer questions resolving independently, timeout exit, non-question pass-through, malformed-payload validation. New test-story `implementations/tests-stories/0001-superpowers-e2e-validation.md` documents 3 humanize-step scenarios (PP / SD / T each invoking their pre-approved skill in a real session) for human verification post-merge. `commands/manager.md` Skill-question relay subsection gains "Edge cases" sub-paragraph: non-question pass-through (no-op), 5-min relay timeout (status to `*`), malformed-payload validation (status + nudge to peer). Each peer file (PP/SD/T) gains a `Skill({...})` invocation example. T's `humanize_steps` payload on story-done lists 3 scenarios for human relay. First use of `implementations/tests-stories/` directory. Existing sessions pick up on next restart. Just update `.version`. |
| `2.33.1` → `2.33.2` | PreToolUse hook enforces peers-never-AskUserQuestion (programmatic backstop for Story 047 prompt rule). New `scripts/hooks/check-askuserquestion-role.sh` — hook reads its own `$PPID` (= claude session PID directly per spike, no walk needed), reads `.claude/.session-role-by-claude-pid/<pid>` marker; allows iff role == `manager`, else exit 2 with descriptive stderr (peer-routing message for peer markers, self-recovery message for missing/malformed). `.claude/settings.json` gains PreToolUse hook config matching `AskUserQuestion` (uses `$CLAUDE_PROJECT_DIR` env var). All 5 role files (M + PP/SD/T/S) source `scripts/whats-my-role.sh` at startup + call `wow_claim_role <role>` (uses Story 049's helper); exit ceremony calls `wow_release_role`. M Phase 1 stale-file sweep adds `wow_sweep_stale_role_markers`. New test `tests/hook-askuserquestion-role.sh` (8 cases / 20 assertions): manager-allow, peer-deny+routing, missing-deny+self-recovery, malformed gibberish/empty, whitespace-trim allow, stdin tolerance (JSON + empty), parameterized peer-deny for pp/t/s. **Spec deviation:** AC #1 said "new scripts/find-claude-pid.sh" but Story 049 already provides that primitive; v2 plan uses 049's helper instead of duplicating (per SD learning "spec literal vs rationale"). Existing sessions pick up on next restart. Just update `.version`. |
| `2.33.2` → `2.33.3` | Subshell-PPID trap documented in `commands/senior-developer.md` Implementation rules + `commands/tester.md` Hygiene. Doc-only callout: `(bash $HOOK)` parens spawn a subshell that interposes between the caller and the child binary, so the child's `$PPID` lands on the subshell instead of the intended parent. Cost ~5 min to debug on Story 048's hook test. Anti-pattern + fix snippets inline in both role files. SD's Implementation rules gains "Subshell-PPID trap" bullet with worked anti-pattern/fix code blocks; T's Hygiene section gains the same callout for shell-test-writing context (use `{ ... ; }` group instead of `(...)` if multiple statements need bundling). Existing sessions pick up on next peer restart. Just update `.version`. |
| `2.33.3` → `2.33.4` | New lint test `tests/pp-checkpoint-status-shape.sh` (10 cases / 13 assertions) covers PP `pp-checkpoint` payload shape (Story 043). Inline `validate_pp_checkpoint` mirrors what M's pp-checkpoint handler should require: 4 required fields (`items_reviewed_so_far`, `open_reviews_now`, `last_finding_count_per_item`, `bus_cursor_line_number_observed`) + types (array, array, object, number). Cases 1-8 cover present/missing/wrong-type combinations; cases 9-10 verify the M-side `tracker.pp_checkpoints` ring-buffer trim-to-10 jq logic (FIFO: oldest dropped, latest preserved, length == 10 after 11 appends). T's coverage gap from sprint 2026-05-02-role-primitive retro. Test-only — no role-file or runtime changes. Just update `.version`. |
| `2.33.4` → `2.33.5` | Bus-emit hygiene promoted from observation to HARD RULE across all 5 role files (M + SD/PP/T/S). SD's Implementation rules gains a "Bus-emit hygiene" bullet with full prose: rich-text payloads (apostrophes, backticks, multi-line, free-text >40 chars) MUST go through `jq --arg field "$value"` for every text field OR Write+jq-slurp via `jq --argjson payload "$(cat /tmp/payload.json)"` — NEVER inline single-quoted JSON. M's Bus overview gains the M-flavored rule (status digests, retro digests, AskUserQuestion bodies). PP/T/S each gain a one-line reference pointing back to SD's full prose. New test `tests/bus-emit-hygiene.sh` (6 cases / 11 assertions) demonstrates the trap (inline-with-apostrophe → malformed JSON; inline-with-backtick → bash command-substitution) and verifies both fix patterns (jq --arg + Write+slurp) preserve content byte-for-byte. SD's jq-apostrophe failure has recurred across 2 consecutive sprints despite filing the observation in retro — promoting to hard rule. Existing sessions pick up on next restart. Just update `.version`. |
| `2.33.5` → `2.33.6` | Closes v2.6.0 baseline-propagation gap: `/implementations/\.github/` added to PP + T fswatch baselines in BOTH the MUST-include enumeration AND the example shape (`commands/pair-programmer.md` line 69+example block, `commands/tester.md` line 77+example block). Root cause: v2.6.0 added `/implementations/\.github/` to `_agent-protocol.md` "Universal baseline" only, but PP/T role files present a shorter MUST-include checklist + a bash example shape that agents copy from at startup — both omitted `.github/`, so every PP/T spawn silently dropped the exclude and the GitHub bridge's per-PR cursor writes (49 PRs × 30s polling ≈ 50 events/min) flooded the monitor. New regression test `tests/role-fswatch-baseline.sh` (8 cases / 8 assertions): 4 positive cases (PP+T MUST-include + example shape), 1 protocol-baseline sanity, 3 negative-scope sanity (M/SD/S have no fswatch arm, so don't need the exclude). Existing PP+T sessions pick up on next restart (fswatch re-arm with full baseline). Just update `.version`. |
| `2.33.6` → `2.33.7` | `scripts/sprint-merge-bump.sh` manifest auto-discovery fix (Story 056). Old line 49 used `ls -1d ... \| head -1` which picked the alphabetically OLDEST sprint manifest (e.g. `2026-05-01-quality-of-life`) instead of the current one. Hit in production during sprint hygiene-batch — wrapper failed with "no manifest item for branch <X>" because it was looking in the wrong sprint. New `_discover_manifest()` function: (1) honors `WOW_SPRINT_MANIFEST` env override, (2) primary jq-filter for `.status == "in-progress"` (one match → use it), (3) multi-active fails loud (exit 2 + stderr lists conflicting paths — M coordinates one sprint at a time), (4) tail -1 fallback when no sprint is in-progress (graceful degradation for one-off backlog promotions). Wrapper gains a sourceable guard (`[ "${BASH_SOURCE[0]:-$0}" != "$0" ] && return 0`) so the function can be unit-tested in isolation. New test `tests/sprint-merge-bump-manifest-discovery.sh` (5 cases / 9 assertions): single-in-progress, multi-with-one-active (proves alphabetical-first bypassed), no-active-tail-fallback, multi-active-fails-loud, env-override-wins. Single-story (single-story_mode in story-created), patch bump. Just update `.version`. |
| `2.33.7` → `2.33.8` | Plugin update-availability check + cross-agent hello-mismatch detector (Story 057). Two informational features surface to the human as direct text output (NOT bus messages) — both non-blocking, no `AskUserQuestion` interrupts. (1) New helper `scripts/check-plugin-updates.sh` queries `gh release view --repo nedati-technologies/claude-wow-plugin` for the latest stable, semver-compares to local `plugin.json`, prints `update-available <local> <latest> <url>` if newer (silent on match / gh-failure / non-semver tags). M's Phase 1 gains step 10 — runs the helper once per session after version-coherence repair, prints "⚡ Plugin update available: ..." line on stdout if newer; tracker schema gains `last_update_check_ts` (auto-init `null`, stamped only). (2) M's `hello` event handler extended with version-mismatch detection: extracts `Plugin v(X.Y.Z)` from peer hello payload (handles both string and `{note:...}` shapes), compares to local `plugin.json`, emits `nudge` to peer's exact agent ID + prints "⚠ Version drift: ..." line on stdout when peer is on a different version. In-memory `nudged_agents` set suppresses re-nudge spam (one nudge per agent ID per M session). Soft hello-payload-version convention added to `commands/_agent-protocol.md` "Hello" subsection. New test `tests/version-coherence-detector.sh` (7 cases / 7+ assertions): 3 helper cases (match/newer/gh-failure) via PATH-overridden `gh` shim + 4 hello-detector cases (match/drift-emits-one/duplicate-suppressed/object-payload-extracts-too) via prompt-described-logic simulator. Existing M sessions pick up on next restart. Just update `.version`. |
| `2.33.8` → `2.34.0` | PostToolUse activity log + M-side observability-based liveness (Story 058, Theme L from hook-conversion-opportunities.md). New PostToolUse hook `scripts/hooks/log-activity.sh` matches `Write\|Edit\|MultiEdit\|Bash\|Read\|Grep\|Glob` and appends `{ts, claude_pid, role, tool}` JSONL to `${ROOT}/implementations/.activity.jsonl` per tool call. Hook is fire-and-forget — exits 0 unconditionally on every error path (observability MUST NOT block tool execution); marker missing → silent skip (unlike AskUserQuestion hook which blocks). Rotation: every 100th call, trim to last 24h via tmp + mv when log ≥ 1000 lines (BSD `date -v-24H` with GNU `-d '24 hours ago'` fallback). New helper `scripts/m-activity-summary.sh` reads the log and emits `{by_role: {<role>: <last_ts|null>}, total_lines_since: <int>, since: <iso>}` JSON; defaults `since` to now-5min; sourceable + CLI. M's Pre-sleep liveness round gains an "Activity-log first" subsection (additive — `run_liveness_round()` and slow-cron fallback unchanged): consult activity log; if PP/SD/T are all activity-alive in last 5 min, skip ping entirely; otherwise fall through to ping for missing roles only. Pre-sleep gate AND-combines bus-silence + activity-silence (>30 min). Team-idle check (autonomy-pickup gate) updated to consult activity log first. New `_agent-protocol.md` "Activity log" subsection documents schema/path/producer/consumer/privacy/rotation. `.gitignore` adds `/implementations/.activity.jsonl` + `/implementations/.activity-counter` (runtime state, never committed). New test `tests/activity-log-liveness.sh` (8 cases / 8+ assertions): hook (3 cases — marker-present, marker-missing, malformed-stdin), reader (3 — empty/mixed/since-arg), rotation (1 — >1000 lines + counter at 100), integration (1 — round-trip producer→consumer). Existing sessions pick up the hook on next restart. Just update `.version`. |
| `2.34.0` → `2.35.0` | Convergence rename `senior-dev-*` → `senior-developer-*` for SD's bus-addressing convention (Story 059, Theme N sub PP#O from hook-conversion-opportunities-v2.md + human directive). The SD role file was `commands/senior-developer.md` but the addressing-glob/agent-ID prefix used the abbreviated `senior-dev-*` form — the only inconsistent role. Natural-instinct typo (writing `senior-developer-*` because that matches the file name) caused silent message-loss via bus-tail filter drop. This row converges everything on the full form. **Backwards-compat transition layer in `scripts/bus-tail.sh`:** when `ROLE == "senior-developer"`, the filter ALSO matches `to: "senior-dev-*"` (legacy glob) AND exact old-form IDs (`to: "senior-dev-X"`); receive routing is `to`-only (never `from` — would false-positive on cross-agent traffic). Each legacy `to` match emits `[bus-tail-deprecated-glob] to=… from=…` to stderr as telemetry. Remove the transition layer in v2.36 or later (after all peers migrated). **M Phase 1 step 6 gains a one-time tracker rename:** `mv .agents/senior-dev-*.json` (and `.bus-tail-cursor` siblings) to `senior-developer-*` so a running pre-rename SD session's tracker survives the merge without losing state. Idempotent. **All 5 role files updated:** `senior-developer.md` (15 occurrences inc. agent-ID-prefix prose + bus-tail Monitor invocation), `manager.md` (19), `_agent-protocol.md` (13), `pair-programmer.md` (5), `tester.md` (5). Append-only history (retros/historical bus dumps/old plans) untouched per non-goals. **After-migration restart reminder for SD:** SD running pre-merge prompt continues using `senior-dev-` agent ID; restart picks up the new prefix. Transition layer covers the in-flight session window. New regression test `tests/senior-developer-rename.sh` (5 cases / 5+ assertions): bus-tail role=senior-developer accepts new + legacy globs + new + legacy exact-IDs, stderr deprecation fires for legacy matches. Existing sessions pick up on next restart. Just update `.version`. |
| `2.35.0` → `2.36.0` | Plugin hooks moved from project to plugin layer + canonical env var (Story 060). v2.33.2 (Story 048 PreToolUse AskUserQuestion) and v2.34.0 (Story 058 PostToolUse activity log) inadvertently shipped hooks at the WRONG layer (project `.claude/settings.json`) using the WRONG env var (`$CLAUDE_PROJECT_DIR`) — only worked in this dogfooding repo where the project IS the plugin source; downstream consumers got ZERO hook enforcement. **Architecture corrected:** new `hooks/hooks.json` at plugin root (canonical path per Claude Code plugins-reference.md docs — the file lives one level above the `.claude-plugin/` metadata dir, not inside it); both hook commands now use `${CLAUDE_PLUGIN_ROOT}` (canonical env var per docs lines 532-556 — substituted at hook-spawn time AND exported to subprocesses). Project `.claude/settings.json` `hooks` block REMOVED (kept `enabledPlugins` only). **Consumer action required after upgrade:** if you manually copied the v2.33.2/v2.34.0 hooks block into your own project's `.claude/settings.json`, REMOVE it — Claude Code merges hooks across layers additively, so the duplicate would cause double-fires (each Bash/Edit/Read tool would log activity twice; each AskUserQuestion would block twice with stderr noise). After upgrade run `/reload-plugins` so Claude Code picks up the new plugin-shipped hooks file. **Defensive guard added** to both hook scripts: `PLUGIN_DIR="${CLAUDE_PLUGIN_ROOT:-$(cd "$(dirname "$0")/.." && pwd)}"` — derives plugin root from `$0` if env var is unset (legacy/older Claude Code); future hook scripts that need to source sibling helpers (e.g., `whats-my-role.sh`) can rely on `$PLUGIN_DIR`. New regression test `tests/plugin-hooks-shape.sh` (5 cases / 9 assertions): canonical-path-exists + wrong-path-absent, both PreToolUse + PostToolUse declared, all commands use `${CLAUDE_PLUGIN_ROOT}` (no `$CLAUDE_PROJECT_DIR` / `$CLAUDE_PLUGIN_DIR` references), project settings has no `hooks` block (jq `has("hooks") | not`), env-var fallback works (script doesn't crash when `CLAUDE_PLUGIN_ROOT` is unset). **Auto-merge revoked** (per human directive 2026-05-04): M emits PR-nudge, SD opens PR via `gh pr create`, human merges manually via GitHub UI. M does NOT call `gh pr merge` or `scripts/sprint-merge-bump.sh` for the merge step. Existing sessions pick up the new plugin-shipped hooks on next `/reload-plugins`. Just update `.version`. |
| `2.36.0` → `3.0.0` | Plugin MCP server + atomic rewrite of all 6 directive files + cross-role skill-creator authority (Story 062). **First MCP server in the plugin** at `mcp/claude-wow-server/server.py` (stdlib-only Python, JSON-RPC 2.0 stdio); declared in `.mcp.json` at plugin root using `${CLAUDE_PLUGIN_ROOT}`. **First MCP tool:** `mcp__claude-wow__bus_emit` — strict input validation (from regex, type enum incl. 47 + 2 new types `behavioral-change-flag`/`behavioral-change-cleared`, to glob/ID), atomic JSONL append. Project root resolved via `$CLAUDE_PROJECT_DIR` (set by Claude Code at MCP-server spawn) with 8-level walk-up fallback to `.claude-plugin/plugin.json` or `.git`. **Naturally fixes worktree-ROOT bug from backlog 087** — server runs from project root context, not worktree-cwd. **Atomic rewrite of all 6 directive files** (commands/{manager,senior-developer,pair-programmer,tester,slacker,_agent-protocol}.md) per skill-creator 5-principle checklist (atomic, action-oriented, self-contained, current-state-only, discoverable triggers): drop body-level "(introduced in v…)" sediment from headers + inline prose; replace all raw `>>` direct bus writes with documented `mcp__claude-wow__bus_emit` JSON args shapes; replace all bash bus-emit.sh script invocations with MCP equivalent; drop Story 059 transitional tracker-rename block (idempotent, ran 2+ versions); drop bare legacy `senior-dev` role-enum refs in protocol file. Migration table itself untouched (canonical changelog by design — historical rows reference paths/scripts that may have been removed in subsequent stories). **Cross-role skill-creator authority subsection** added to all 5 role files: M/SD/PP/T/S may invoke `Skill('skill-creator:skill-creator')` and `Skill('superpowers:writing-skills')` when authoring/editing/auditing markdown directive files. PP applies the checklist during plan-review and post-impl review whenever SD's diff touches a directive file. **Behavioral-change gate** added to protocol type table: SD emits `behavioral-change-flag` to PP before committing protocol-level changes; PP returns `behavioral-change-cleared` after inspection. **Deleted** the legacy bus-emit script from Story 061, superseded by the MCP tool. **Test bench:** `tests/agent-state-tracking.sh` retargets 7 bus-emit cases to MCP fixture (case 15 repurposed as negative test — no env fallback); new `tests/mcp-server-bus-emit.sh` (6 cases / 13 assertions) covering tools/list shape, valid emit, invalid type/to, payload preservation, missing-from rejection; new `tests/fixtures/mcp-call.sh` fixture; new `tests/directive-files-atomic.sh` (6 cases / 18 assertions) gating each directive file for ZERO stale patterns (raw `>>` writes, bash bus-emit.sh refs, bare `senior-dev` refs) in non-migration-row content. **Suite count: 55 → 57.** **Backlog hygiene:** 084 (tool-conversion sprint) + 087 (worktree-ROOT bug) + 088 (peers skipping superpowers — partial via cross-role authority) marked `<!-- status: promoted -->`. **Auto-merge revoked** per the v2.36.0 row's auto-merge-revoked note: human merges this PR manually via GitHub UI. **Consumer action required after upgrade:** run `/reload-plugins` so Claude Code picks up the new plugin-shipped MCP server; re-read role files to refresh mental model (every role file body atomically rewritten). Existing sessions pick up on next restart. Just update `.version`. |
| `3.0.0` → `3.0.1` | Cleanup of 4 residual `<NEXT-to>` placeholder annotations that escaped Story 062's atomic rewrite (PP FINDING-3, non-blocking). Story 063, source backlog `implementations/backlog/091-cleanup-residual-next-to-placeholders.md`. **Fixes:** `commands/manager.md` L1093 / L1108 / L1135 (Story 061 header annotations) replaced with literal `3.0.0`; `commands/tester.md` L161 (`expected_suite_count` back-compat note) replaced with literal `2.29.0`. **Regression test:** `tests/directive-files-atomic.sh` extended with `check_no_residual_placeholders` case (+6 assertions, one per directive file) that grep-fails any `<NEXT-to>`/`<NEXT-from>` occurrence outside hardcoded allowed line-ranges (SD's plan-conventions block ~L170-300, PP's code-review check ~L270-290). Suite count unchanged at 57 (extending existing test). Auto-merge revoked: SD substituted version literals + new migration row stamp explicitly (no `<NEXT-from>`/`<NEXT-to>` placeholders in this row). Just update `.version`. |
| `3.0.1` → `3.0.2` | M release-scan prior-merge detection (Story 064, source backlog `implementations/backlog/094-m-stale-status-re-release-trap.md`). Real incident 2026-05-07: M's Phase 1 + cron-tick proactive-release re-released Story 053 to SD because the story file's `<!-- status: -->` line was never flipped from `filed` to `done` after PR #50 merged 2026-05-02. ~10 min of SD/PP/T cycle wasted. **Defense layer:** new helper `scripts/m-prior-merge-detect.sh <NNN> <slug>` — greps main's commit history before any release-scan emits `story-created` to SD. Two-tier match: Tier 1 explicit story-id reference in merge subject (`feat\(0*<NNN>\)`, `story 0*<NNN>`, `0*<NNN>:`) → `MATCH`; Tier 2 `(#<digits>)` merge whose PR head was `feat/0*<NNN>-` (via `gh pr view --json headRefName`) → `AMBIGUOUS`. No match → `NONE`. **M's branching:** `MATCH` → auto-flip status to done + append story-done trailer + standing-authority commit + skip release; `AMBIGUOUS` → AskUserQuestion (auto-flip / release-as-is / investigate-manually); `NONE` → proceed with normal release. **Test:** `tests/m-release-scan-prior-merge-detection.sh` (5 cases / 8 assertions): clear MATCH, no-match NONE, AMBIGUOUS via gh shim, idempotency, gh-missing fallback. **Suite count: 57 → 58.** Auto-merge revoked: SD substituted version literals + migration-row stamp explicitly. M-only logic, no peer behavior change. Just update `.version`. |
| `3.0.2` → `3.0.3` | Orphan-cron dedup at Phase 3 cron-arm + cron-tick belt-and-braces (Story 065, source backlog `implementations/backlog/090-orphan-cron-on-monitor-kill-restart.md`). Real incident 2026-05-07T15:50: M had two `<<autonomous-loop>>` crons firing every 5 min (silent token-cost doubling) due to monitor-kill + re-bootstrap leaving a prior session's cron alive. **Pre-arm dedup at Phase 3 step 7:** `CronList` → `CronDelete` every entry whose prompt is `<<autonomous-loop>>` BEFORE the new `CronCreate`. Idempotent across restarts. **Belt-and-braces at cron-tick step 0** (Per-tick processing): scan via `CronList`; if more than one `<<autonomous-loop>>` entry, delete all but `tracker.cron_id`; emit diagnostic `status` to `*` via `mcp__claude-wow__bus_emit`. Edge: if `tracker.cron_id` is null/absent (tick fired before Phase 3 completed), delete ALL orphans and let next Phase 3 re-arm. **New subsection** `## Orphan-cron dedup` in `## Cron lifecycle` between Wake-on-activity trigger and On clean exit. **Test:** `tests/cron-dedup-shape.sh` (4 cases / 10 assertions, doc-shape grep against `commands/manager.md`); `CronList`/`CronDelete` are Claude Code primitives, not bash-testable. Suite count 58 → 59. M-only behavior; no peer-side change. Auto-merge revoked. Just update `.version`. |
| `3.0.3` → `3.0.4` | Document upstream `code-review:code-review` plugin haiku dedup false-positive (Story 066, source backlog `implementations/backlog/093-code-review-plugin-haiku-dedup-false-positive.md`). Real incident 2026-05-07 PR #61: the `claude-code-review.yml` workflow ran SUCCESS but produced NO `claude[bot]` review comment because the plugin's haiku pre-check tripped on a prior empty-body `state: COMMENTED` review by a non-bot author. Mode A (silent-skip) is the primary observation surface; Mode B (mis-attributed AI-prose comment) is rarer. **Pure-docs change:** new `docs/superpowers/specs/2026-05-07-upstream-claude-code-plugins-haiku-dedup-issue.md` drafts the upstream-issue body for filing on `anthropics/claude-code-plugins`. **PP awareness note** in `commands/pair-programmer.md` "Handling external review signals" → new `## Upstream code-review plugin haiku dedup false-positive` subsection covering Mode A (no triage to fabricate) + Mode B (mark not_actionable) with explicit cite to the spec draft. **M doc note** in `commands/manager.md` Bridge events `pr-comment` handler — 1-paragraph note keyed to Mode A so M doesn't generate spurious bus traffic when the workflow silent-skips. **No workflow changes** (`.github/workflows/claude-code-review.yml` is already correctly configured per Story 055 era; the bug is upstream). **Test:** `tests/directive-files-atomic.sh` extended with 2 new assertions (PP note presence + spec-path cite). Suite count UNCHANGED at 59 (extending existing test file, not adding a new suite per PP option-a confirm). **Humanize step on story-verified:** human reviews the draft and either files upstream or marks `<!-- decided-not-to-file -->`. Auto-merge revoked; SD substituted version literals + this row stamp explicitly. Just update `.version`. |
| `3.0.4` → `3.0.5` | `/install-github-app` workflow defaults: fixup helper + smoke test + upstream issue draft (Story 067, source backlog `implementations/backlog/077-install-github-app-workflow-defaults.md`). Anthropic-shipped `/install-github-app` slash command installs `.github/workflows/claude-code-review.yml` with broken defaults: missing `--comment` in prompt + read-only PR/issue permissions → silent runs that cost ~$1/PR but post nothing. Story 055 audit empirically reproduced (3 runs on PR #55, $5.50 wasted). **NEW** `scripts/wow-install-github-app-fixup.sh` (idempotent sed-based YAML patcher, 5 mechanical fixes: append `--comment`, set `pull-requests: write`, set `issues: write`, add `use_sticky_comment: true`, add `display_report: true`; cleans up its own `.bak` artifact per PP). **NEW** `tests/install-github-app-workflow-shape.sh` (5 grep assertions; skip-if-absent guard so consumers without the workflow exit 0 silently). **`commands/manager.md` Phase 1 step 11** (new): if workflow-shape FAILs, print informational warning suggesting the fixup script — non-blocking, opt-in (no auto-run). **NEW** `docs/superpowers/specs/2026-05-07-upstream-install-github-app-defaults-issue.md` Markdown draft for filing against `anthropics/claude-code-action` — cites Story 055 cost data + suggested 5-key/flag fix. **Suite count: 59 → 60.** Auto-merge revoked. Stacked on feat/066. Per PP FINDING-7: no `(introduced in vX.Y.Z, Story 067)` markers in new prose. Just update `.version`. **REVERTED in v3.0.6** — see next row. |
| `3.0.5` → `3.0.6` | Revert Story 067's WOW-internal artifacts (out-of-scope: `/install-github-app` is a Claude Code slash command and a downstream-consumer concern, not a WOW responsibility — some users use the GitHub app, some don't, and WOW shouldn't bake that decision into M's startup). **Removed:** `scripts/wow-install-github-app-fixup.sh`, `tests/install-github-app-workflow-shape.sh`, `commands/manager.md` Phase 1 step 11 (workflow-shape check). **Suite count: 60 → 59.** **Kept:** `docs/superpowers/specs/2026-05-07-upstream-install-github-app-defaults-issue.md` (the upstream-issue draft remains as a doc artifact for the human to file separately if desired). Story 067 file flipped to `<!-- status: reverted -->` with a trailer noting the scope-creep reason. Backlog 077 keeps its `<!-- status: promoted -->` for historical accuracy + a `<!-- post-revert-note -->` block. M-only revert; no peer behavior change. Auto-merge revoked. Just update `.version`. |
| `3.0.6` → `3.1.0` | Tester redesign (off fswatch) + cross-agent token-conservation doctrine (Story 069, design spec `docs/superpowers/specs/2026-05-08-tester-redesign-and-token-discipline-design.md`; amendments 1+2+3+4 squashed into this row). **NEW** `commands/_token-discipline.md` (canonical doctrine, project-agnostic, 8 sections; allows `model: opus` for harder bounded work where Sonnet underperforms — per amendment-4, the prior "Never `model: opus`" rule is dropped). Per amendment-2 (backlog 095 WOW-genericity): doctrine ships project-agnostic; per-role concrete delegation examples live project-side at `implementations/learnings/<role>.md`. **Per amendment-3 — MCP server auto-injects** the doctrine refresh: `mcp/claude-wow-server/server.py` `bus_emit` handler writes BOTH the original line AND a parallel `read-token-discipline` broadcast (`to: *`, `payload: {path: "commands/_token-discipline.md", reason: "auto-injected after <type>"}`) in a SINGLE `f.write(serialized + inject_serialized)` call within one `with open` block, whenever `type IN {story-created, sprint-kickoff}`. POSIX append-mode atomicity for writes ≤ PIPE_BUF means both lines or neither — concurrent writers' pairs never interleave. ALLOWED_TYPES enum gains `sprint-kickoff` + `read-token-discipline`. **Per amendment-4 — strip role-file prose (mechanical-over-prose):** each of the 5 role files now has ONE LINE for token-discipline in its session-startup procedure: `Read commands/_token-discipline.md.` That is the entire role-file footprint. NO `# Token discipline` section. NO bus-handler entries for `read-token-discipline`. NO mention of the MCP server in role-file prose. The MCP server enforces the refresh in code. M's role file additionally drops the 4 emit-prose sites (Interactive behavior story-created recipe, queued-story release mechanics, sprint Phase 3 dispatch, Phase 2 sprint-kickoff Step 1) and the doctrine-edit manual-emit edge case — peers refresh on the next dispatch's auto-injected broadcast. **Schema:** `commands/_agent-protocol.md` adds new bus message type `read-token-discipline` (auto-injected by MCP server → `*`, payload `{path, reason}`); the `story-created` payload schema is unchanged (no `token_discipline_doctrine` field). **Tester (heaviest change):** drops fswatch (verify/exclude/arm) — bus-tail Monitor is now the only Monitor. "Reacting to events" + "fswatch lore" sections deleted. "Testability concerns" renamed `(post-impl)`; T's `story-done` handler gains a first sub-step that reads SD's diff (`git diff <merge-base>..HEAD`) through testability lens + emits `testability-concern` with `path:line` before test-story authoring. T runs on Opus. **Tests:** `tests/token-discipline-doctrine-shape.sh` (22 assertions; case 6 inverts to allow `model: opus` per amendment-4); `tests/mcp-server-bus-emit-auto-inject.sh` (RUNTIME test, 17 assertions via `tests/fixtures/mcp-call.sh` shim: 10 runtime — story-created/sprint-kickoff each yield 2 lines, story-done yields 1 line, auto-inject envelope shape, single-write atomicity verified by 5-way concurrent emit producing 10 lines with zero pair-interleaving — plus 7 doc-shape: protocol row, 5 role-file startup-reads, negative `_agent-protocol.md` story-created schema check; the bus-handler doc-shape from amendment-3 is dropped per amendment-4 since role files no longer carry that prose); `tests/directive-files-atomic.sh` reshaped per amendment-4 (5 doctrine-file refs, T-fswatch + post-impl-rename, NEW negative — role files do NOT have `# Token discipline` section header; amendment-2 dual-citation + section-scoped negatives + amendment-3 manual-emit-paired negatives all removed since the underlying section is gone). **DELETED** `tests/read-token-discipline-broadcast.sh` (doc-shape only — superseded). **Suite count: 59 → 61** (-1 deleted, +2 new). **Consumer action required after upgrade:** (1) restart all peers via `/reload-plugins` then `/<role>` for each — running peers have stale prompts and won't pick up the role-file changes or T's fswatch removal until restart; (2) the MCP server reload happens via `/reload-plugins` too — without it the auto-inject is dormant and dispatch types won't fan out the doctrine refresh. Auto-merge revoked: SD substituted version literals + this row stamp explicitly. Just update `.version`. |
| `3.1.0` → `3.2.0` | Retro doctrine + MCP auto-inject (mechanical-over-prose for retros, Story 070, mirrors Story 069 amendment-3 pattern). **NEW** `commands/_retro-doctrine.md` (project-agnostic; 6 canonical sections: Trigger condition, Multi-party flow, Etiquette, Learnings-refresh window, Action items to backlog, Sprint manifest status flip; M-only writes). **MCP server** (`mcp/claude-wow-server/server.py`) gains parallel auto-inject branch: `RETRO_AUTO_INJECT_TRIGGERS = frozenset(["review-closed", "retro-open"])` + `RETRO_DOCTRINE_PATH = "commands/_retro-doctrine.md"`. ALLOWED_TYPES gains `read-retro-doctrine` (`review-closed` + `retro-open` were already present). Disjoint from existing token-discipline trigger set; non-conflicting `elif` branch in `handle_bus_emit`. Same single-`f.write()` atomic-append discipline. **Schema:** `commands/_agent-protocol.md` gains the `read-retro-doctrine` row (auto-injected by MCP server → `*`). **Role-file strip:** all 5 role files lose retro-behavioral prose. Each gains ONE startup-read pointer for `commands/_retro-doctrine.md` mirroring the token-discipline pointer pattern. M loses entire Phase 4 — Retro section + Sprint retro etiquette section. SD/PP/T/S lose `# Sprint-end learnings refresh` + `# Sprint retro etiquette` sections. PP retains `# Sprint review-closed signal` (protocol message documentation, not retro behavior). **Tests:** new `tests/retro-doctrine-coherence.sh` (26 assertions: file existence, 6 canonical section headers, length 50-300, 5-role pointer references, negative strip checks, PP-retains-review-closed, schema row present); new `tests/mcp-server-retro-auto-inject.sh` (12 assertions runtime via mcp-call.sh shim: review-closed/retro-open each yield 2 lines, non-trigger yields 1 line, auto-inject envelope shape, single-write atomicity via 5-way concurrent emit, token-discipline auto-inject coexistence regression guard). **Suite count: 61 → 63** (+2 new test files). **Consumer action required after upgrade:** restart all peers via `/reload-plugins` then `/<role>` for each — running peers have stale prompts and won't pick up the stripped role files until restart; the MCP server reload also happens via `/reload-plugins` — without it the new auto-inject branch is dormant. Auto-merge revoked: SD substituted version literals + this row stamp explicitly. Single-story dispatch (no sprint-mode placeholders). Just update `.version`. |
| `3.2.0` → `3.3.0` | WOW process modularization + PID-file uniqueness (Story 071, source backlog `implementations/backlog/085-pid-file-uniqueness-for-monitors-crons-bridges.md`; design spec `docs/superpowers/specs/2026-05-13-wow-process-pid-uniqueness-design.md`). **NEW** directory `scripts/wow-process/` with 3 wrapper scripts shipped (slack-bridge.sh descoped per PP FINDING-11 — bridge writes its own .bridge-pid + does its own port-collision check, wrapper is redundant): `bus-tail.sh` (relocated from `scripts/bus-tail.sh` via `git mv`, PID-uniqueness preamble prepended, conflict policy `kill`); `fswatch-peer.sh` (extracted from PP role-file inline recipe, conf-file loadable excludes, conflict policy `kill`); `github-bridge.sh` (wraps `python3 bridge/github/run.py`, conflict policy `raise` — exit 2 on port collision so caller can escalate). **PID file naming per-role** per PP FINDING-9 (cross-agent kill mitigation): `${PURPOSE}-${ROLE}.pid` not `${PURPOSE}.pid` — multiple roles spawning the same purpose (bus-tail) get isolated PID files; bus-tail kill policy no longer creates cross-agent kills. **NEW** `scripts/wow-process/role-process-map.json` — single source of truth for which wrapped scripts each role runs (consumed by Story 072 PostCompact hook + future tooling). **`scripts/bus-tail.sh`** retained as 3-line backwards-compat shim that `exec`s the new location. **Config file convention:** each script auto-reads `${ROOT}/implementations/.wow-process/<purpose>.conf` if present (sourceable bash; absence = WOW base defaults). **Caller migration** in 5 role files (path swap only): M Phase 3 step 4 (bus-tail) + step 5 (github-bridge); SD/PP/T/S startup bus-tail; PP startup fswatch (inline recipe replaced by wrapper invocation). Slack bridge spawn in S unchanged (descoped per FINDING-11). **NEW** `tests/wow-process-pid-uniqueness.sh` (18 assertions: 7 spec cases + per-role isolation regression guard for FINDING-9 + manifest-coherence pairs). **MODIFIED** `tests/bus-tail-predicate.sh`, `tests/bus-tail-cursor.sh`, `tests/bus-restore-handshake.sh`, `tests/senior-developer-rename.sh` — path updates only, behavior assertions unchanged. **MODIFIED** `.gitignore` adds `/implementations/.wow-process/*.pid`. **MODIFIED** `.claude-plugin/plugin.json` description sheds the internal-path reference per PP nit (was `via scripts/bus-tail.sh`, now `via the bundled bus-tail filter`). **DISMISSED** `implementations/backlog/098-pp-fswatch-self-prune-on-noise.md` (subsumed by `scripts/wow-process/fswatch-peer.sh` + per-project `fswatch-peer.conf`). **Suite count: 63 → 64** (+1 new test). **Consumer action required after upgrade:** restart all peers via `/reload-plugins` then `/<role>` for each — running peers reference the old `scripts/bus-tail.sh` path inline; the shim covers them but a clean restart re-arms via the new wrapper directly. Auto-merge revoked (sprint `auto_merge: false`): SD substituted version literals + this row stamp explicitly. Just update `.version`. |
| `3.3.0` → `3.4.0` | PostCompact hook → mechanical WOW process restore (Story 072, source backlog `implementations/backlog/066-monitor-cleanup-after-context-compaction.md`; design spec `docs/superpowers/specs/2026-05-13-post-compact-process-restore-design.md`). **NEW** MCP server CLI shim in `mcp/claude-wow-server/server.py`: argv-driven dispatch (`python3 server.py bus_emit --from <id> --to <id> --type <type> [--payload-json <json>]`) for hook subprocess invocation. Reuses `handle_bus_emit` for validation + atomic append. Exit codes: 0 success, 2 validation error, 3 IO error. **NEW** bus message type `compaction-occurred` in ALLOWED_TYPES + `commands/_agent-protocol.md` schema row (`{agent_id, role, ts}` payload; hook (self-emit) → `<self-agent-id>`). **NEW** `scripts/hooks/wow-post-compact-bus-notice.sh` — reads session-keyed role marker (`.claude/.session-role-by-claude-pid/$PPID`, same convention as `log-activity.sh`/`check-askuserquestion-role.sh`), looks up agent_id, emits `compaction-occurred` via CLI shim with SYNTHETIC sender (`postcompact-hook-<ts>-<6hex>`) so bus-tail self-echo filter does not drop the line. Silent exit 0 on every failure path (observability hook). **NEW** `scripts/wow-process/post-compact-restore.sh` — reads role-process-map.json for current role, for each purpose checks `${WOW_PROCESS_DIR}/<purpose>-<role>.pid` (per Story 071 PP-FINDING-9 naming) + emits `ALIVE <p> <pid>` or `MISSING <p>`. Exit 0 success, 2 map missing, 3 role marker missing. **NEW** `hooks/hooks.json` `"hooks"."PostCompact"` entry (registered at the repo-convention location; `.claude-plugin/plugin.json` does NOT register Claude Code hooks). **NEW** one-line `compaction-occurred` handler in all 5 role files (M/SD/PP/T/S) directing the agent to run `post-compact-restore.sh` and re-arm MISSING wrappers via 071. **NEW** `tests/post-compact-process-restore.sh` (18 assertions: 8 spec cases + per-role PID-naming regression guard + bus-tail-actually-forwards-the-line regression guard for codex BLOCKER). **MODIFIED** `mcp/claude-wow-server/server.py` ALLOWED_TYPES + main block. **MODIFIED** `.claude-plugin/plugin.json` `version` 3.3.0 → 3.4.0. **MODIFIED** `implementations/.version` 3.3.0 → 3.4.0. **MODIFIED** `commands/manager.md` "Plugin version" header 3.3.0 → 3.4.0. **Suite count: 64 → 65** (+1 new test). **Consumer action required after upgrade:** restart all peers via `/reload-plugins` then `/<role>` for each — running peers have stale role-file handler tables and won t pick up the `compaction-occurred` handler until restart; MCP server reload also via `/reload-plugins` — the new CLI dispatch + bus type are loaded then. Auto-merge revoked (sprint `auto_merge: false`): SD substituted version literals + this row stamp explicitly. Just update `.version`. |
| `3.4.0` → `3.5.0` | Bus writes MCP-only — mechanical enforcement + plain-text fallback (Story 073, source backlog `implementations/backlog/100-bus-mcp-only-with-human-fallback.md`; design spec `docs/superpowers/specs/2026-05-13-bus-mcp-only-enforcement-design.md`). **NEW** PreToolUse hook `scripts/hooks/wow-forbid-direct-bus-write.sh` (~60 LOC bash) — blocks direct writes to `${ROOT}/implementations/.message-bus.jsonl` for `Bash` (regex match of `>>`/`>`/`tee`/`sed -i` followed by the bus path on the right) and `Write`/`Edit`/`MultiEdit`/`NotebookEdit` (file_path match). False-positive guard: `cat bus > other` is allowed (the regex requires the bus path on the RIGHT of the write operator). Exit 2 on match with canonical stderr message pointing at the MCP tool + fallback doctrine. Graceful fallback if `jq` missing (stderr warning, exit 0). **NEW** `commands/_mcp-failure-fallback.md` (~50 lines) — canonical doctrine: when MCP fails, output a plain-text message to the human in the Claude UI asking them to `/mcp restart`. Explicitly does NOT call `AskUserQuestion` — Story 048 hook blocks it for non-M peers; plain-text reaches the human at the same latency without any guardrail conflict. Why-not section cites Story 048 hook + role-routing model. **NEW** `hooks/hooks.json` PreToolUse entry (second array item alongside the existing AskUserQuestion entry) with matcher `Bash|Write|Edit|MultiEdit|NotebookEdit`. **NEW** `tests/forbid-direct-bus-write.sh` (14 assertions: 9 spec cases + MultiEdit coverage + `cat bus > other` false-positive guard + jq-missing graceful fallback). **5 role files (M/SD/PP/T/S)** lose the "Bus-emit via MCP tool (HARD RULE)" prose paragraph; gain a one-line pointer ("Bus writes are MCP-only. The PreToolUse hook ... blocks direct writes. On MCP failure follow commands/_mcp-failure-fallback.md."). Same mechanical-over-prose pattern Stories 069/070/072 established. **Suite count: 65 → 66**. **Consumer action required after upgrade:** restart all peers via `/reload-plugins` then `/<role>` for each — stale prompts retain the old HARD RULE paragraph (no broken behavior, but stale wording). MCP server reload not needed for this story (no server changes). Auto-merge revoked (sprint `auto_merge: false`): SD substituted version literals + this row stamp explicitly. Just update `.version`. |
| `3.5.0` → `3.6.0` | CI quality gates (Story 074, no source backlog — human directive 2026-05-13). **NEW** `.github/workflows/tests.yml` — BLOCKING; runs `bash tests/run-all.sh` on every `pull_request` (opened/synchronize/reopened) + every `push` to `main`. Ubuntu-latest runner; verifies pre-installed deps (bash/jq/python3/curl/gh) without re-installing. **NEW** `.github/workflows/shellcheck.yml` — warn-only initially (`continue-on-error: true`); uses `ludeeus/action-shellcheck@2.0.0` against `scripts/` + `hooks/` + `tests/` with severity=warning. **NEW** `.github/workflows/markdownlint.yml` — warn-only initially; uses `DavidAnson/markdownlint-cli2-action@v17` scoped to `commands/*.md` + `docs/**/*.md` + `implementations/learnings/*.md` (workflow-artifact dirs — stories/plans/backlog/bugs/tests-stories/sprints — explicitly excluded). **NEW** `.github/workflows/python-lint.yml` — warn-only initially; uses `astral-sh/ruff-action@v3` against `mcp/claude-wow-server/server.py` + `bridge/github/run.py`. All 4 workflows use YAML structured trigger form (`on: pull_request: types: [...]`; bare-list form is invalid per GHA schema, PP plan-review MAJOR-1). All 4 use `permissions: contents: read` only (no write). Existing `claude-code-review.yml` unchanged per human directive 2026-05-13. **Upstream issue filed** at https://github.com/anthropics/claude-code-action/issues/1308 documenting Mode A (gh CLI invocation without --repo) + Mode B (haiku dedup false-positive on no-findings reviews); body cross-refed in `implementations/backlog/099-github-bridge-webhook-thrash.md`. **3 follow-up backlog items** for M to file at story-ship (per `followup_backlog_action` payload field): promote shellcheck / markdownlint / python-lint to blocking after baseline cleanup. **Webhook-flake CI convention** (carried from sprint 2026-05-13 retro): tests.yml is blocking; if only `tests/github-bridge-webhook-mode.sh` fails on CI, ONE rerun is acceptable; if it fails on rerun too, escalate to human (do not merge). **Suite count: 66 → 66** (no new test files in this story). **Consumer action required after upgrade:** no consumer-side action needed; this story only adds repo-side CI surface. Auto-merge revoked (single-story dispatch, not sprint mode): SD substituted version literals + this row stamp explicitly. Just update `.version`. |
| `3.6.0` → `3.7.0` | Plugin / project separation + release-via-dist-branch convention (Story 075, no source backlog — human directive 2026-05-13). Plugin deliverable moved into `plugin/` subfolder of the source repo. Released to consumers via `git subtree split --prefix=plugin -b dist-staging` → `git push --force-with-lease origin dist-staging:dist` → tag → GitHub release; new `scripts/release-dist.sh` (~150 LOC bash; stays at source-repo root, NOT bundled) automates this with trap cleanup, idempotency check (refuses re-tag), content verification (`git ls-tree dist-staging` asserts inclusion + exclusion before push), `--force-with-lease` (not bare `-f`), and a `--dry-run` flag. **Consumer marketplace URL changes:** `/plugin marketplace remove <current>` then `/plugin marketplace add nedati-technologies/claude-wow-plugin@dist`; long-form fallback `git+ssh://git@github.com/nedati-technologies/claude-wow-plugin.git@dist`. **Scripts split** (per PP review): 13 source-repo project tools (sprint-merge-bump, sprint-graph-next-dispatchable, sprint-manifest-validate, sprint-rebase-cascade, m-prior-merge-detect, m-state-compare, m-activity-summary, file-story-from-backlog, wow-bus-restore, wow-set-expected-state, wow-storage, whats-my-role, check-plugin-updates) stay at source-repo `scripts/` and are NOT bundled; only runtime-needed `scripts/wow-process/`, `scripts/hooks/`, and `scripts/bus-tail.sh` shim move to `plugin/scripts/`. Tests partition similarly: ~12 project-tool tests stay at root `tests/`, ~55 plugin tests move to `plugin/tests/`. **Path-fixups** in `scripts/sprint-merge-bump.sh` (`PLUGIN_JSON` → `plugin/.claude-plugin/plugin.json`; `MGR_MD` → `plugin/commands/manager.md`; `git show :plugin/...` prefix) and `scripts/check-plugin-updates.sh` (try-`plugin/`-then-fallback `.claude-plugin/plugin.json` lookup). **NOT MOVED** (per PP BLOCKER-1): `implementations/.version` stays at `implementations/.version` — per-project runtime state, NOT a plugin deliverable. **NEW** `tests/dist-tree-shape.sh` (at source-repo root) — runs subtree-split locally and asserts top-level dirs + scripts subdirs match the expected boundary (two-tier `git ls-tree`: top for `commands`/`scripts`/etc; `:scripts` for `wow-process`/`hooks`/`bus-tail.sh` shim). **NEW** `plugin/AGENTS.md`, `plugin/CLAUDE.md`, `plugin/README.md` — consumer-facing copies. Root `AGENTS.md` rewritten as project-side dev rules + repo layout. **CI workflows** updated: `tests.yml` runs BOTH `bash tests/run-all.sh` AND `cd plugin && bash tests/run-all.sh`; `shellcheck.yml` scandir extended to `./scripts ./tests ./plugin/scripts ./plugin/hooks ./plugin/tests`; markdownlint + python-lint globs prefixed with `plugin/`. **Test pathfix:** plugin-side tests that reference stay-at-root scripts (`activity-log-liveness`, `agent-state-tracking`, `backlog-promotion-coherence`, `bus-restore-handshake`, `version-coherence-detector`, `plugin-hooks-shape`, `command-cross-refs`, `superpowers-relay`, `peers-never-askuserquestion`) updated to derive `SOURCE_ROOT="$(cd "$REPO_ROOT/.." && pwd)"` and target source-repo root for non-bundled scripts. **Suite count: 66 → ~67** (split into ~12 root + ~55 plugin + 1 new `dist-tree-shape.sh`). **First v3.7.0 dist bootstrap** by SD during the impl smoke-test (step 13a); subsequent releases auto-fire via `bash scripts/release-dist.sh` after each version-bumping merge. **Consumer action required after upgrade:** one-shot marketplace URL migration (see commands above); next `claude plugin update` pulls from `@dist`. Auto-merge revoked (single-story dispatch, not sprint mode): SD substituted version literals + this row stamp explicitly. Just update `.version`. |
| `3.7.0` → `3.7.1` | MCP server declaration moved from root `.mcp.json` to `plugin/.claude-plugin/plugin.json` `mcpServers` block (per `eb796fb`). Same MCP path; plugin-scoped registration rather than project-scoped — consumers no longer need a root-level `.mcp.json` to pick up the `bus_emit` tool. Plus two doc artifacts: CLAUDE.md typo fix and `implementations/learnings/tester.md` documenting the post-075 two-runner test pattern (root + plugin suites, 67 suites total at this point). Pure patch — no behavior change for already-installed consumers. **Consumer action required after upgrade:** none. Just update `.version`. |
| `3.7.1` → `3.7.2` | Convention files cleanup (per `4e07ad2`). Sharpened the boundary between contributor-facing rules (root `CLAUDE.md` / `AGENTS.md` / `README.md`) and consumer-facing rules (`plugin/CLAUDE.md` / `plugin/AGENTS.md` / `plugin/README.md`). Root files own repo layout, test-coverage matrix, release flow; plugin files own role summary, runtime requirements, install URL. Doc-only patch; no behavior change. **Consumer action required after upgrade:** none. Just update `.version`. |
| `3.7.2` → `3.8.0` | Monitor architecture rebuild (PR #78, design spec `docs/superpowers/specs/2026-05-14-monitor-architecture-rebuild-design.md`, plan `docs/superpowers/plans/2026-05-14-monitor-architecture-rebuild.md`). Replaces Story 061's `expected-state/` + `actual-state/` + cron check-in with an event-driven monitor process. **NEW** `plugin/scripts/wow-process/manager-monitor.{sh,py}` — bash wrapper with PID-uniqueness preamble + Python loop (60s interval, `kill -0` PID liveness, scans `.activity.jsonl` tail). When every live wow-process PID in `{manager, senior-developer, pair-programmer, tester}` has most-recent row `type ∈ {stop, stop_failure}` AND `implementations/.nothing_to_do` is absent, monitor emits `all-idle-nudge` to M via the MCP server's `bus_emit` CLI. **NEW** MCP tools `mcp__claude-wow__declare_idle` + `mcp__claude-wow__resume_work` (M-only by convention) — sticky do-not-disturb marker at `implementations/.nothing_to_do`; the activity hook never auto-clears it. **NEW** UserPromptSubmit hook `wow-remind-resume-work.sh` injects a `hookSpecificOutput.additionalContext` reminder when marker present + role is manager (empirically confirmed CC delivers the JSON as a `<system-reminder>` block). **Rewritten** `plugin/scripts/hooks/log-activity.sh` — single 6-event router (PreToolUse, UserPromptSubmit, Stop with `last_assistant_message`, StopFailure, SessionStart, SessionEnd) replacing the previous PostToolUse-only hook + Story 061's `set-state-{active,chilling}.sh`. Schema extended: `{ts, claude_pid, role, type, tool?, text?}`. **NEW** `all-idle-nudge` entry in MCP `ALLOWED_TYPES`. **REMOVED** `plugin/scripts/hooks/set-state-{active,chilling}.sh`, `scripts/m-state-compare.sh`, `scripts/wow-set-expected-state.sh`, `plugin/tests/{activity-log-liveness,agent-state-tracking}.sh`. **MODIFIED** `tests/dist-tree-shape.sh:74` drops `m-state-compare.sh` from the expected-scripts enum. **MODIFIED** `plugin/commands/manager.md` — replaces cron-based check-in prose with monitor launch + one inline directive about implicit `resume_work` on user re-engagement. **MODIFIED** `plugin/commands/_agent-protocol.md` Liveness section rewritten. **NEW tests:** `activity-log-schema.sh`, `monitor-predicate.sh`, `monitor-marker-shortcircuit.sh`, `monitor-pid-liveness.sh`, `monitor-nudge-bus-shape.sh`, `declare-idle-tool.sh`, `wow-remind-resume-work.sh`. **Suite count: 60 → 62** (-2 deleted, +7 new in plugin suite). Magic numbers collapse from {5min cron, 10min staleness, 3-strike counter} to one (60s monitor loop). **Consumer action required after upgrade:** restart all peers via `/reload-plugins` then `/<role>` for each — running peers retain stale hook scripts and won't pick up the router or the new MCP tools until restart. The MCP server reload happens via `/reload-plugins` too — without it `declare_idle` / `resume_work` won't surface. |
| `3.8.0` → `3.9.0` | PP review timing redesign (PR #79, design spec `docs/superpowers/specs/2026-05-14-pp-review-timing-design.md`, plan `docs/superpowers/plans/2026-05-14-pp-review-timing.md`). PP's realtime fswatch pathway produced predominantly low-signal findings (mid-flight code is half-broken or in transition). Replaces it with event-driven reviews at six bus checkpoints with three distinct review depths. **Checkpoints:** `plan-ready-for-review` (pre-impl plan critique, existing), `plan-done` (per-plan line-level review, existing), `story-done` (NEW — holistic story-level AC review; SD's emit now addresses `pair-programmer-*` alongside `tester-*` + `manager-*`), sprint meta-review (NEW — pattern-level cross-story pass added BEFORE PP's existing `review-closed` emit, output under `## Sprint meta-review <date>` header in `.review.txt`), `bug-verified` (existing), `nudge` w/ `pr-review`/`pr-comment` payload (existing GitHub-bridge external triage). **REMOVED** fswatch pathway entirely: `plugin/scripts/wow-process/fswatch-peer.sh`, `plugin/tests/role-fswatch-baseline.sh`, `fswatch-peer` entry in `role-process-map.json` PP row, all fswatch sections in `pair-programmer.md` (verify/exclude/lore/flood/`[changed]`-handler/batch-related-events prose, ~57 lines net). **NEW** tests: `story-done-recipients.sh`, `pp-event-driven-reviews.sh`, `pp-no-fswatch.sh`. **MODIFIED** `plugin/commands/_agent-protocol.md` story-done routing row + dropped T-fswatch references (`Monitor excludes (T only)` learnings bullet, `Monitor exclusion` subsection, `fswatch CLI` required-env bullet, the entire `Monitor composition (T)` section — T's role file already only arms bus-tail Monitor, those refs were stale). **MODIFIED** `plugin/commands/{manager,senior-developer,slacker}.md` — drop fswatch env-dep, atomic-rename comment, negative "you don't run an fswatch monitor" wording, slacker step 9 verify-fswatch (renumbered 10/11/12 → 9/10/11). **MODIFIED** `plugin/AGENTS.md` + `plugin/README.md` drop fswatch from runtime requirements. **Total-chill mechanism deprecated and removed everywhere** (no one currently emits it): TOTAL_CHILL_MODE sections deleted from PP/SD/T role files; `total-chill` / `total-chill-end` / `total-chill-ack` removed from MCP `ALLOWED_TYPES`. **MODIFIED** `plugin/tests/directive-files-atomic.sh` line-range guard shifted by removed lines. **Suite count: 62 → 62** (+3 new, -1 removed via fswatch baseline). **Consumer action required after upgrade:** restart all peers via `/reload-plugins` then `/<role>` for each — running peers retain stale prompts; PP will continue arming fswatch via the cached wrapper until restart (no harm, just dead Monitor). MCP server reload also via `/reload-plugins` to pick up the trimmed `ALLOWED_TYPES`. |
| `3.9.0` → `3.9.1` | Migration-table backfill + never-skip rule (PR #80). The migration table had silently fallen 4 rows behind (3.7.0 → 3.9.0 transitions undocumented). This patch backfills all four rows (3.7.0→3.7.1 MCP declaration cleanup, 3.7.1→3.7.2 convention files cleanup, 3.7.2→3.8.0 monitor architecture rebuild PR #78, 3.8.0→3.9.0 PP review timing PR #79) plus this self-referential 3.9.0→3.9.1 row. Root `CLAUDE.md` gains a new rule: every plugin version bump MUST add a migration row — even patch bumps. Doc-only patch; no plugin behavior change. **Consumer action required after upgrade:** none. Just update `.version`. Follow-up: a test that asserts `plugin/.claude-plugin/plugin.json` version has a matching row in the migrations file would mechanically enforce the rule. |
| `3.9.1` → `3.10.0` | Boot procedure split + cron-prose deletion + sentinel hook. **NEW** `plugin/scripts/hooks/wow-inject-startup.sh` — UserPromptSubmit hook that detects `<!-- claude-wow-startup: <role> -->` sentinel in the first 5 lines of a slash-command-expanded prompt and emits `additionalContext` instructing the agent to Read `plugin/commands/_<role>-startup.md`. CC delivers `additionalContext` as a `<system-reminder>` (same mechanism as `wow-remind-resume-work.sh`). **NEW** `plugin/commands/_<role>-startup.md` for each of `manager`, `senior-developer`, `pair-programmer`, `tester`, `slacker` — boot procedure (claim role, required reading, setup, peer check, bootstrap) moved out of `commands/<role>.md`. **MODIFIED** each `commands/<role>.md`: sentinel comment prepended on line 1; Phase 1/2/3 + migration block + setup-on-startup removed; cron prose deleted (`# Cron lifecycle`, `# Scheduled check-in`, `Pre-sleep liveness round`, `Orphan-cron dedup`, `cron_id`/`cron_cadence`/`quiet_ticks`/`last_liveness_round_*` tracker fields, clean-exit `CronDelete`, "re-arm cron / wake-on-activity / fast-cron / slow-cron" references). The 3.8.0 monitor-architecture rebuild replaced cron with the event-driven `manager-monitor` but the cron prose survived; this migration completes the deletion. **MODIFIED** `plugin/hooks/hooks.json` — adds `wow-inject-startup.sh` under `UserPromptSubmit` (3 hooks now: log-activity, wow-remind-resume-work, wow-inject-startup). **MODIFIED** `plugin/tests/version-coherence.sh` — grep target moves from `commands/manager.md` to `commands/_manager-startup.md`. **MODIFIED** `plugin/tests/directive-files-atomic.sh` — doctrine-file-ref assertions point at `_<role>-startup.md`; `<NEXT-to>` placeholder allowed line-ranges re-baselined to current SD/PP line numbers. **MODIFIED** `plugin/tests/mcp-server-bus-emit-auto-inject.sh` + `plugin/tests/retro-doctrine-coherence.sh` — doctrine-file-ref assertions point at `_<role>-startup.md`. **NEW tests:** `wow-inject-startup.sh` (9 cases — 5 roles, missing sentinel, sentinel-past-line-5, missing startup file, wrong event type), `command-files-have-startup-sentinel.sh` (line-1 sentinel guard per role), `startup-files-exist-and-claim-role.sh` (every `_<role>-startup.md` exists + contains `wow_claim_role <role>` invocation). **REMOVED** `plugin/tests/cron-dedup-shape.sh` — assertions targeted cron-dedup prose in manager.md; obsolete after cron-prose deletion. **Role-marker claim ordering change:** `wow_claim_role <role>` was Phase 3 step 1 (or a sub-bullet inside it) in each role; now it is step 1 of `_<role>-startup.md` — claimed BEFORE any `AskUserQuestion` in Phase 1 migration confirm or Phase 2 peer wait (the `check-askuserquestion-role.sh` PreToolUse hook gates AUQ on the marker; old order had a chicken-and-egg risk if Phase 1 needed AUQ — now mooted). **Migration-table read-on-demand discipline:** the migration playbook block stays inside `_manager-startup.md`, prefixed with "Run this entire step ONLY if `.version` differs from target". Agents that don't migrate never read `plugin/docs/superpowers/migrations/manager-schema-migrations.md`. **Suite count:** 62 → 64 (+3 new, −1 deleted). **Consumer action required after upgrade:** `/reload-plugins` then restart all peers via `/<role>` for each — running peers retain stale prompts; new slash-command invocations will pick up the sentinel + the hook will inject the Read instruction. Without restart, peers will continue running Phase-1/2/3 prose pinned in their context. Just update `.version`. |
| `3.10.0` → `3.11.0` | Remove UserPromptSubmit startup-injection hook; explicit boot reference in role files (Story 077). The v3.10.0 hook (`wow-inject-startup.sh` UserPromptSubmit hook + line-1 `<!-- claude-wow-startup: <role> -->` sentinel + `additionalContext` injection) didn't reliably surface inside `/<role>` slash-command expansions — observed empirically (v3.10.0 ship session). Replaces the hook with an explicit boot-procedure paragraph at the top of each role-command file (immediately after the YAML frontmatter, before the role body). **DELETED** `plugin/scripts/hooks/wow-inject-startup.sh` (33 lines). **DELETED** `plugin/tests/wow-inject-startup.sh` (the 9-case hook test). **DELETED** `plugin/tests/command-files-have-startup-sentinel.sh` (line-1 sentinel guard — obsolete after sentinel removal). **MODIFIED** `plugin/hooks/hooks.json` — removed the third `UserPromptSubmit` entry wiring `wow-inject-startup.sh`. Other UserPromptSubmit hooks remain (`log-activity.sh`, `wow-remind-resume-work.sh`). UserPromptSubmit entry count: 3 → 2. **MODIFIED** each of the 5 role-command files (`plugin/commands/{manager,senior-developer,pair-programmer,tester,slacker}.md`): dropped the line-1 `<!-- claude-wow-startup: <role> -->` sentinel + its trailing blank line; inserted a "Boot procedure" paragraph immediately after the YAML frontmatter referencing `commands/_<role>-startup.md`; dropped the trailing `"Begin now: read CLAUDE.md / AGENTS.md / _agent-protocol.md / learnings/<role>.md, run startup..."` sentence (now covered by the Boot paragraph). **NEW** test `plugin/tests/role-files-reference-startup.sh` — for each of the 5 role-command files, asserts (a) the file contains the literal `commands/_<role>-startup.md`, (b) the file does NOT contain the legacy `claude-wow-startup` sentinel literal anywhere (regression guard), (c) the file's first non-blank line is NOT the legacy sentinel. 20 assertions total (4 per role × 5 roles). **Untouched:** `plugin/commands/_<role>-startup.md` files (content unchanged), `plugin/.claude-plugin/plugin.json` `version` literal (M's merge wrapper stamps at PR-merge time), `plugin/commands/_manager-startup.md` "Plugin version" literal (same placeholder convention). **Suite count: 65 → 64** (+1 new, −2 deleted). **Consumer action required after upgrade:** `/reload-plugins` + optionally restart roles via `/<role>` for each — running peers retain their old in-context prompts (sentinel + trailing sentence still in memory), but they continue working unchanged since the hook is also removed; new role invocations after `/reload-plugins` pick up the explicit Boot paragraph in the role-command body. Change is additive in flight; no data migration. Just update `.version`. |
