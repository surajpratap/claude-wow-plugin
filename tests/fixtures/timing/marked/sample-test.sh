#!/usr/bin/env bash
# Lint fixture (Story 170): a TIGHT ceiling (20 < floor 30) but explicitly
# annotated with the allow-marker. lint-timing-ceilings.sh MUST PASS this line.
wait_for f p 1 20  # generous
