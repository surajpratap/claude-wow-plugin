diff --git a/mcp/claude-wow-server/server.py b/mcp/claude-wow-server/server.py
index 6fda05d..6e468f0 100755
--- a/mcp/claude-wow-server/server.py
+++ b/mcp/claude-wow-server/server.py
@@ -519,7 +519,7 @@ def handle_bus_emit(args):
                 "from": from_id,
                 # AHOD: the relay is suspended — the PR author owns its own
                 # review pass, so the cue routes back to the emitter.
-                "to": from_id if mode == "ahod" else "pair-programmer-*",
+                "to": "pair-programmer-*",
                 "type": "code-review-request",
                 "payload": {
                     "reason": f"auto-injected after {msg_type}",
