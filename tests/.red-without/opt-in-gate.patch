diff --git a/scripts/wow-process/manager-monitor.py b/scripts/wow-process/manager-monitor.py
index 4f59f0b..b705b3f 100755
--- a/scripts/wow-process/manager-monitor.py
+++ b/scripts/wow-process/manager-monitor.py
@@ -603,10 +603,7 @@ def usage_concern(project_root):
     (usage-limit / usage-reset / usage-escalate) instead of emitting bus
     directives — M owns the urgent-stop reaction. Idempotent via the pause-marker.
     """
-    # --- USAGE-OPTIN-GATE-START (opt-in-gate.patch reverts this block) ---
-    if not usage_autopause_enabled(project_root):
-        return
-    # --- USAGE-OPTIN-GATE-END ---
+    # REVERT opt-in-gate: gate removed — acts regardless of the opt-in flag.
     state = _load_json_file(usage_state_path(project_root))
     if state is None:
         return
