diff --git a/scripts/wow-process/manager-monitor.py b/scripts/wow-process/manager-monitor.py
index 4f59f0b..0c5fad3 100755
--- a/scripts/wow-process/manager-monitor.py
+++ b/scripts/wow-process/manager-monitor.py
@@ -503,7 +503,7 @@ def marker_present(project_root):
 # ---------------------------------------------------------------------------
 # Story 172 — opt-in usage auto-pause (additive limit codepath).
 # ---------------------------------------------------------------------------
-FIVE_HOUR_PAUSE_THRESHOLD = 95
+FIVE_HOUR_PAUSE_THRESHOLD = 98  # REVERT 186-threshold-9598
 SEVEN_DAY_ESCALATE_THRESHOLD = 99
 # Small buffer past resets_at before the daemon emits resume — the state file
 # is stale at >=98 until the first post-reset render, so we lead slightly to
