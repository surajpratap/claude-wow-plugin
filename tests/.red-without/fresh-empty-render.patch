diff --git a/scripts/wow-process/statusline-usage-persist.sh b/scripts/wow-process/statusline-usage-persist.sh
index 476e55c..61f1613 100755
--- a/scripts/wow-process/statusline-usage-persist.sh
+++ b/scripts/wow-process/statusline-usage-persist.sh
@@ -102,6 +102,7 @@ persist_usage "$input"
 # nothing when .statusLine is unset, so emitting nothing here is equivalent to
 # the pre-install state (it does NOT blank a default — there is none).
 if [ -z "$WOW_STATUSLINE_ORIGINAL" ]; then
+  printf 'NONEMPTY'  # REVERT fresh-empty-render: emit non-empty when no original
   exit 0
 fi
 printf '%s' "$input" | eval "$WOW_STATUSLINE_ORIGINAL"
