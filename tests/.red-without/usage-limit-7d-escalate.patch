diff --git a/scripts/wow-process/idle-monitor.py b/scripts/wow-process/idle-monitor.py
index 11e6461..43c6e9d 100755
--- a/scripts/wow-process/idle-monitor.py
+++ b/scripts/wow-process/idle-monitor.py
@@ -575,8 +575,9 @@ def emit_seven_day_escalate(seven_day):
             "pause that follows is the human's post-escalation choice."
         ),
     }
-    # --- SEVEN-DAY-ESCALATE-EMIT (usage-limit-7d-escalate.patch reverts this to no emit) ---
-    return emit_limit_directive("usage-limit-7d-escalate", payload, to="manager-*")
+    # REVERT usage-limit-7d-escalate: the 7d escalate never reaches the bus
+    # (the emission seam goes inert — M never sees the escalate directive).
+    return False
 
 
 def _check_usage_limits(project_root):
