diff --git a/scripts/wow-process/manager-monitor.py b/scripts/wow-process/manager-monitor.py
index 4f59f0b..0aa4122 100755
--- a/scripts/wow-process/manager-monitor.py
+++ b/scripts/wow-process/manager-monitor.py
@@ -642,7 +642,7 @@ def usage_concern(project_root):
     if marker is not None:
         resets_at = marker.get("resets_at")
         resets_epoch = parse_iso_ts(resets_at)
-        if resets_epoch is not None and now >= resets_epoch + RESET_BUFFER_SECONDS:
+        if resets_epoch is not None:  # REVERT 186-usage-time-resume: drop the time gate
             if pipe_usage_event("usage-reset", {"kind": "usage-reset", "window": "five_hour"}):
                 try:
                     os.unlink(marker_path)
