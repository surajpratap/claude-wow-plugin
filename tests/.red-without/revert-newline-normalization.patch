diff --git a/plugin/scripts/hooks/_rm-glob-detect.py b/plugin/scripts/hooks/_rm-glob-detect.py
index 466cefd..9e8897c 100644
--- a/plugin/scripts/hooks/_rm-glob-detect.py
+++ b/plugin/scripts/hooks/_rm-glob-detect.py
@@ -39,6 +39,7 @@ def base(t):
 
 
 def _normalize(cmd):
+    return cmd  # REVERT revert-newline-normalization: skip multiline/continuation handling
     # Newline normalization (5 steps, in order). shlex + whitespace_split eats \n as
     # whitespace (never a token), so multi-line structure must be encoded first — but
     # \n is NOT always a separator: a backslash-line-continuation `\<nl>` the shell
